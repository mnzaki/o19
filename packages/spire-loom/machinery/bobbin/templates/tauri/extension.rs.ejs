//! AppHandle Extension - GENERATED by spire-loom
//!
//! Extension trait for accessing the <%= coreName %> platform from AppHandle.
//!
//! Generated from <%= coreName %> Management definitions.

use tauri::{Manager, Runtime};
use crate::spire::platform::<%= platformTraitName %>;

/// Extension trait for accessing <%= coreName %> platform.
pub trait <%= extensionTraitName %><R: Runtime> {
  /// Access the <%= coreName %> platform implementation.
  fn <%= platformMethodName %><'a>(&'a self) -> &'a dyn <%= platformTraitName %>;
}

impl<R: Runtime, T: Manager<R>> <%= extensionTraitName %><R> for T {
  fn <%= platformMethodName %><'a>(&'a self) -> &'a dyn <%= platformTraitName %> {
    self.state::<crate::spire::<%= platformStructName %>>().inner().platform()
  }
}
