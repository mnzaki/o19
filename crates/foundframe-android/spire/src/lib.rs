// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/packages/spire-loom/machinery/bobbin/android/jni_bridge.jni.rs.ejs
// Template file: jni_bridge.jni.rs.ejs
// To override: Copy this template to loom/bobbin/android/jni_bridge.jni.rs.ejs
// 
// To modify the generated output, edit the template file above.
/**
 * JNI Bridge for RustExternalLayerRadicleService
 * 
 * This module provides the JNI bindings between the Android Kotlin service
 * and the Rust core library.
 * 
 * Architecture:
 * - Kotlin holds an opaque handle (jlong) to the Rust service
 * - Each JNI call locks the service, executes, then unlocks
 * - The handle pattern ensures thread-safe access to the core
 * 
 * Generated for core: RustExternalLayer
 */

use jni::objects::{JClass, JString};
use jni::signature::JavaType;
use jni::sys::{jint, jlong, jboolean};
use jni::JavaVM;
use jni::JNIEnv;
use log::info;
use std::ffi::c_void;
use std::sync::{Arc, Mutex, OnceLock};
use RustExternalLayer as core;

// Type alias for the service handle used by JNI
// The core crate's main type is assumed to be RustExternalLayer
pub type ServiceHandle = core::RustExternalLayer;

/// Global service handle storage
/// 
/// Pattern: OnceLock<Arc<Mutex<T>>>
/// - OnceLock: Thread-safe one-time initialization
/// - Arc: Shared ownership across JNI calls  
/// - Mutex: Exclusive access during operations
/// 
/// The handle is initialized by nativeStartService and used by all
/// subsequent native method calls.
static SERVICE_HANDLE: OnceLock<Arc<Mutex<core::ServiceHandle>>> = OnceLock::new();

/// JNI registration function called when the native library is loaded
#[no_mangle]
pub extern "C" fn JNI_OnLoad(_vm: JavaVM, _reserved: c_void) -> jint {
    android_logger::init_once(
        android_logger::Config::default()
            .with_max_level(log::LevelFilter::Debug),
    );
    jni::sys::JNI_VERSION_1_6
}

/// Start the native service
/// 
/// Called from Kotlin: nativeStartService(homeDir, alias)
/// Returns: handle ID (always 1 for now, or -1 on error)
/// 
/// This initializes the core service and stores the handle globally.
/// The returned handle ID is passed back to all subsequent native calls.
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeStartService(
    mut env: JNIEnv,
    _class: JClass,
    home_dir: JString,
    alias: JString,
) -> jlong {
    let home_dir: String = env
        .get_string(&home_dir)
        .expect("Failed to get home_dir")
        .into();
    
    let alias: String = env
        .get_string(&alias)
        .expect("Failed to get alias")
        .into();
    
    info!("[RustExternalLayerRadicleService] Starting native service: home_dir={}, alias={}", home_dir, alias);
    
    match initialize_service(&home_dir, &alias) {
        Ok(handle) => {
            let _ = SERVICE_HANDLE.set(Arc::new(Mutex::new(handle)));
            info!("[RustExternalLayerRadicleService] Native service started successfully");
            1
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] Failed to start native service: {}", e);
            -1
        }
    }
}

/// Stop the native service
/// 
/// Called from Kotlin when the service is being destroyed.
/// This drops the service handle, cleaning up resources.
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeStopService(
    _env: JNIEnv,
    _class: JClass,
    _handle: jlong,
) {
    info!("[RustExternalLayerRadicleService] Stopping native service");
    
    // OnceLock can't be cleared, but we can drop the inner value
    // by taking the mutex guard and replacing with None
    if let Some(handle) = SERVICE_HANDLE.get() {
        if let Ok(mut guard) = handle.lock() {
            // The handle will be dropped when the guard is released
            info!("[RustExternalLayerRadicleService] Service handle dropped");
        }
    }
}

/// Initialize the core service
/// 
/// Creates the actual core::ServiceHandle by calling into the core crate.
fn initialize_service(
    home_dir: &str, 
    alias: &str
) -> Result<core::ServiceHandle, Box<dyn std::error::Error>> {
    info!("[RustExternalLayerRadicleService] Initializing core service");
    
    // TODO: Replace with actual core initialization
    // core::ServiceHandle::new(home_dir, alias)
    unimplemented!("Service initialization not yet implemented in core")
}

/// Execute an operation with the service handle locked
/// 
/// This is the core pattern: lock → execute → unlock
/// All command methods use this to ensure thread-safe access.
fn with_service<F, R>(f: F) -> Result<R, String>
where
    F: FnOnce(&mut core::ServiceHandle) -> R,
{
    let handle = SERVICE_HANDLE
        .get()
        .ok_or_else(|| "Service not initialized".to_string())?;
    
    let mut guard = handle
        .lock()
        .map_err(|_| "Failed to lock service handle".to_string())?;
    
    Ok(f(&mut *guard))
}

/// BookmarkMgmt.bookmark_addBookmark
/// 
/// JNI Signature: BookmarkAddBookmark(long handle, string, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkAddBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        url: string,
        title: string,
        notes: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let url: string = env.get_string(&url).expect("Failed to get url").into();
        let title: string = env.get_string(&title).expect("Failed to get title").into();
        let notes: string = env.get_string(&notes).expect("Failed to get notes").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_bookmark(url, title, notes))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_add_bookmark failed: {}", e);
            
        }
    }
}

/// BookmarkMgmt.bookmark_getBookmark
/// 
/// JNI Signature: BookmarkGetBookmark(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkGetBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_bookmark(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_get_bookmark failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// BookmarkMgmt.bookmark_getBookmarkByUri
/// 
/// JNI Signature: BookmarkGetBookmarkByUri(long handle, string) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkGetBookmarkByUri(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        uri: string
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        let uri: string = env.get_string(&uri).expect("Failed to get uri").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_bookmark_by_uri(uri))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_get_bookmark_by_uri failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// BookmarkMgmt.bookmark_listBookmarks
/// 
/// JNI Signature: BookmarkListBookmarks(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkListBookmarks(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_bookmarks(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_list_bookmarks failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// BookmarkMgmt.bookmark_updateBookmark
/// 
/// JNI Signature: BookmarkUpdateBookmark(long handle, number, string, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkUpdateBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        title: string,
        notes: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let title: string = env.get_string(&title).expect("Failed to get title").into();
        let notes: string = env.get_string(&notes).expect("Failed to get notes").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.update_bookmark(id, title, notes))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_update_bookmark failed: {}", e);
            0
        }
    }
}

/// BookmarkMgmt.bookmark_deleteBookmark
/// 
/// JNI Signature: BookmarkDeleteBookmark(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeBookmarkDeleteBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_bookmark(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] bookmark_delete_bookmark failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_addConversation
/// 
/// JNI Signature: ConversationAddConversation(long handle, JString, JString, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationAddConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        content: JString,
        captureTime: JString,
        title: string,
        sourceUrl: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let content: String = env.get_string(&content).expect("Failed to get content").into();
        let captureTime: String = env.get_string(&captureTime).expect("Failed to get captureTime").into();
        let title: string = env.get_string(&title).expect("Failed to get title").into();
        let sourceUrl: string = env.get_string(&sourceUrl).expect("Failed to get sourceUrl").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_conversation(content, captureTime, title, sourceUrl))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_add_conversation failed: {}", e);
            
        }
    }
}

/// ConversationMgmt.conversation_getConversation
/// 
/// JNI Signature: ConversationGetConversation(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationGetConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_conversation(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_get_conversation failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// ConversationMgmt.conversation_listConversations
/// 
/// JNI Signature: ConversationListConversations(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationListConversations(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_conversations(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_list_conversations failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// ConversationMgmt.conversation_updateConversation
/// 
/// JNI Signature: ConversationUpdateConversation(long handle, number, string, JString) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationUpdateConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        title: string,
        content: JString
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let title: string = env.get_string(&title).expect("Failed to get title").into();
        let content: String = env.get_string(&content).expect("Failed to get content").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.update_conversation(id, title, content))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_update_conversation failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_deleteConversation
/// 
/// JNI Signature: ConversationDeleteConversation(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationDeleteConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_conversation(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_delete_conversation failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_addParticipant
/// 
/// JNI Signature: ConversationAddParticipant(long handle, number, number, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationAddParticipant(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number,
        personId: number,
        role: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
        // personId is already number
        let role: string = env.get_string(&role).expect("Failed to get role").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_participant(conversationId, personId, role))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_add_participant failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_removeParticipant
/// 
/// JNI Signature: ConversationRemoveParticipant(long handle, number, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationRemoveParticipant(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number,
        personId: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
        // personId is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.remove_participant(conversationId, personId))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_remove_participant failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_listParticipants
/// 
/// JNI Signature: ConversationListParticipants(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationListParticipants(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_participants(conversationId))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_list_participants failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// ConversationMgmt.conversation_addConversationMedia
/// 
/// JNI Signature: ConversationAddConversationMedia(long handle, number, number, JString) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationAddConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number,
        mediaId: number,
        context: JString
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
        // mediaId is already number
        let context: String = env.get_string(&context).expect("Failed to get context").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_conversation_media(conversationId, mediaId, context))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_add_conversation_media failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_removeConversationMedia
/// 
/// JNI Signature: ConversationRemoveConversationMedia(long handle, number, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationRemoveConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number,
        mediaId: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
        // mediaId is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.remove_conversation_media(conversationId, mediaId))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_remove_conversation_media failed: {}", e);
            0
        }
    }
}

/// ConversationMgmt.conversation_listConversationMedia
/// 
/// JNI Signature: ConversationListConversationMedia(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeConversationListConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_conversation_media(conversationId))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] conversation_list_conversation_media failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// DeviceMgmt.device_generatePairingCode
/// 
/// JNI Signature: DeviceGeneratePairingCode(long handle) -> string
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceGeneratePairingCode(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    
) -> string {
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.generate_pairing_code())
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_generate_pairing_code failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// DeviceMgmt.device_confirmPairing
/// 
/// JNI Signature: DeviceConfirmPairing(long handle, string, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceConfirmPairing(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        deviceId: string,
        code: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        let deviceId: string = env.get_string(&deviceId).expect("Failed to get deviceId").into();
        let code: string = env.get_string(&code).expect("Failed to get code").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.confirm_pairing(deviceId, code))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_confirm_pairing failed: {}", e);
            0
        }
    }
}

/// DeviceMgmt.device_unpairDevice
/// 
/// JNI Signature: DeviceUnpairDevice(long handle, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceUnpairDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        deviceId: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let deviceId: string = env.get_string(&deviceId).expect("Failed to get deviceId").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.unpair_device(deviceId))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_unpair_device failed: {}", e);
            
        }
    }
}

/// DeviceMgmt.device_listPairedDevices
/// 
/// JNI Signature: DeviceListPairedDevices(long handle) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceListPairedDevices(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    
) -> JString {
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_paired_devices())
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_list_paired_devices failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// DeviceMgmt.device_followDevice
/// 
/// JNI Signature: DeviceFollowDevice(long handle, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceFollowDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        deviceId: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        let deviceId: string = env.get_string(&deviceId).expect("Failed to get deviceId").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.follow_device(deviceId))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_follow_device failed: {}", e);
            0
        }
    }
}

/// DeviceMgmt.device_unfollowDevice
/// 
/// JNI Signature: DeviceUnfollowDevice(long handle, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceUnfollowDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        deviceId: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let deviceId: string = env.get_string(&deviceId).expect("Failed to get deviceId").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.unfollow_device(deviceId))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_unfollow_device failed: {}", e);
            
        }
    }
}

/// DeviceMgmt.device_listFollowers
/// 
/// JNI Signature: DeviceListFollowers(long handle) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceListFollowers(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    
) -> JString {
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_followers())
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_list_followers failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// DeviceMgmt.device_isFollowing
/// 
/// JNI Signature: DeviceIsFollowing(long handle, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeDeviceIsFollowing(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        deviceId: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        let deviceId: string = env.get_string(&deviceId).expect("Failed to get deviceId").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.is_following(deviceId))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] device_is_following failed: {}", e);
            0
        }
    }
}

/// MediaMgmt.media_getMedia
/// 
/// JNI Signature: MediaGetMedia(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaGetMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_media(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_get_media failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaMgmt.media_getMediaByHash
/// 
/// JNI Signature: MediaGetMediaByHash(long handle, string) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaGetMediaByHash(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        contentHash: string
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        let contentHash: string = env.get_string(&contentHash).expect("Failed to get contentHash").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_media_by_hash(contentHash))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_get_media_by_hash failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaMgmt.media_listMedia
/// 
/// JNI Signature: MediaListMedia(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaListMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_media(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_list_media failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaMgmt.media_updateMedia
/// 
/// JNI Signature: MediaUpdateMedia(long handle, number, string) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaUpdateMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        title: string
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let title: string = env.get_string(&title).expect("Failed to get title").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.update_media(id, title))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_update_media failed: {}", e);
            0
        }
    }
}

/// MediaMgmt.media_deleteMedia
/// 
/// JNI Signature: MediaDeleteMedia(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaDeleteMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_media(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_delete_media failed: {}", e);
            0
        }
    }
}

/// MediaMgmt.media_addMediaLink
/// 
/// JNI Signature: MediaAddMediaLink(long handle, string, string, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaAddMediaLink(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        uri: string,
        mimeType: string,
        title: string,
        directory: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let uri: string = env.get_string(&uri).expect("Failed to get uri").into();
        let mimeType: string = env.get_string(&mimeType).expect("Failed to get mimeType").into();
        let title: string = env.get_string(&title).expect("Failed to get title").into();
        let directory: string = env.get_string(&directory).expect("Failed to get directory").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_media_link(uri, mimeType, title, directory))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_add_media_link failed: {}", e);
            
        }
    }
}

/// MediaMgmt.media_addMediaFile
/// 
/// JNI Signature: MediaAddMediaFile(long handle, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaAddMediaFile(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        uri: string,
        title: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let uri: string = env.get_string(&uri).expect("Failed to get uri").into();
        let title: string = env.get_string(&title).expect("Failed to get title").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_media_file(uri, title))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_add_media_file failed: {}", e);
            
        }
    }
}

/// MediaSourceMgmt.media_source_registerSource
/// 
/// JNI Signature: MediaSourceRegisterSource(long handle, string, JString, JString, JString) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceRegisterSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        url: string,
        adapterType: JString,
        capabilities: JString,
        config: JString
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let url: string = env.get_string(&url).expect("Failed to get url").into();
        let adapterType: String = env.get_string(&adapterType).expect("Failed to get adapterType").into();
        let capabilities: String = env.get_string(&capabilities).expect("Failed to get capabilities").into();
        let config: String = env.get_string(&config).expect("Failed to get config").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_register_source(url, adapterType, capabilities, config))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_register_source failed: {}", e);
            
        }
    }
}

/// MediaSourceMgmt.media_source_getSource
/// 
/// JNI Signature: MediaSourceGetSource(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceGetSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_get_source(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_get_source failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaSourceMgmt.media_source_listSources
/// 
/// JNI Signature: MediaSourceListSources(long handle, boolean, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceListSources(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        activeOnly: boolean,
        adapterType: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // activeOnly is already boolean
        let adapterType: String = env.get_string(&adapterType).expect("Failed to get adapterType").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_list_sources(activeOnly, adapterType))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_list_sources failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaSourceMgmt.media_source_updateSource
/// 
/// JNI Signature: MediaSourceUpdateSource(long handle, number, JString, boolean) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceUpdateSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        config: JString,
        isActive: boolean
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let config: String = env.get_string(&config).expect("Failed to get config").into();
        // isActive is already boolean
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_update_source(id, config, isActive))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_update_source failed: {}", e);
            0
        }
    }
}

/// MediaSourceMgmt.media_source_unregisterSource
/// 
/// JNI Signature: MediaSourceUnregisterSource(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceUnregisterSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_unregister_source(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_unregister_source failed: {}", e);
            0
        }
    }
}

/// MediaSourceMgmt.media_source_pollSource
/// 
/// JNI Signature: MediaSourcePollSource(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourcePollSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_poll_source(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_poll_source failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaSourceMgmt.media_source_getHealth
/// 
/// JNI Signature: MediaSourceGetHealth(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceGetHealth(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_get_health(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_get_health failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// MediaSourceMgmt.media_source_resetErrors
/// 
/// JNI Signature: MediaSourceResetErrors(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeMediaSourceResetErrors(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.source_reset_errors(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] media_source_reset_errors failed: {}", e);
            0
        }
    }
}

/// PersonMgmt.person_addPerson
/// 
/// JNI Signature: PersonAddPerson(long handle, string, string, JString) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonAddPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        displayName: string,
        handle: string,
        metadata: JString
    
) -> () {
    
    // Convert JNI parameters to Rust types
        let displayName: string = env.get_string(&displayName).expect("Failed to get displayName").into();
        let handle: string = env.get_string(&handle).expect("Failed to get handle").into();
        let metadata: String = env.get_string(&metadata).expect("Failed to get metadata").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_person(displayName, handle, metadata))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_add_person failed: {}", e);
            
        }
    }
}

/// PersonMgmt.person_getPerson
/// 
/// JNI Signature: PersonGetPerson(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonGetPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_person(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_get_person failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// PersonMgmt.person_getPersonByHandle
/// 
/// JNI Signature: PersonGetPersonByHandle(long handle, string) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonGetPersonByHandle(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        handle: string
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        let handle: string = env.get_string(&handle).expect("Failed to get handle").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_person_by_handle(handle))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_get_person_by_handle failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// PersonMgmt.person_listPeople
/// 
/// JNI Signature: PersonListPeople(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonListPeople(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_people(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_list_people failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// PersonMgmt.person_updatePerson
/// 
/// JNI Signature: PersonUpdatePerson(long handle, number, string, string, JString) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonUpdatePerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        displayName: string,
        handle: string,
        metadata: JString
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let displayName: string = env.get_string(&displayName).expect("Failed to get displayName").into();
        let handle: string = env.get_string(&handle).expect("Failed to get handle").into();
        let metadata: String = env.get_string(&metadata).expect("Failed to get metadata").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.update_person(id, displayName, handle, metadata))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_update_person failed: {}", e);
            0
        }
    }
}

/// PersonMgmt.person_deletePerson
/// 
/// JNI Signature: PersonDeletePerson(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePersonDeletePerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_person(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] person_delete_person failed: {}", e);
            0
        }
    }
}

/// PostMgmt.post_addPost
/// 
/// JNI Signature: PostAddPost(long handle, JString) -> number
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePostAddPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        bits: JString
    
) -> number {
    
    // Convert JNI parameters to Rust types
        let bits: String = env.get_string(&bits).expect("Failed to get bits").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_post(bits))
    }) {
        Ok(result) => {
            result as jint
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] post_add_post failed: {}", e);
            -1
        }
    }
}

/// PostMgmt.post_getPost
/// 
/// JNI Signature: PostGetPost(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePostGetPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_post(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] post_get_post failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// PostMgmt.post_listPosts
/// 
/// JNI Signature: PostListPosts(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePostListPosts(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_posts(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] post_list_posts failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// PostMgmt.post_updatePost
/// 
/// JNI Signature: PostUpdatePost(long handle, number, JString) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePostUpdatePost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number,
        bits: JString
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
        let bits: String = env.get_string(&bits).expect("Failed to get bits").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.update_post(id, bits))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] post_update_post failed: {}", e);
            0
        }
    }
}

/// PostMgmt.post_deletePost
/// 
/// JNI Signature: PostDeletePost(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativePostDeletePost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_post(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] post_delete_post failed: {}", e);
            0
        }
    }
}

/// TheStreamMgmt.the_stream_getEntries
/// 
/// JNI Signature: TheStreamGetEntries(long handle, number, number, JString) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamGetEntries(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        limit: number,
        offset: number,
        filter: JString
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // limit is already number
        // offset is already number
        let filter: String = env.get_string(&filter).expect("Failed to get filter").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_get_entries(limit, offset, filter))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_get_entries failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_getEntry
/// 
/// JNI Signature: TheStreamGetEntry(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamGetEntry(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_get_entry(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_get_entry failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_deleteEntry
/// 
/// JNI Signature: TheStreamDeleteEntry(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamDeleteEntry(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_delete_entry(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_delete_entry failed: {}", e);
            0
        }
    }
}

/// TheStreamMgmt.the_stream_getEntriesByKind
/// 
/// JNI Signature: TheStreamGetEntriesByKind(long handle, JString, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamGetEntriesByKind(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        kind: JString,
        limit: number,
        before: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        let kind: String = env.get_string(&kind).expect("Failed to get kind").into();
        // limit is already number
        // before is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_get_entries_by_kind(kind, limit, before))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_get_entries_by_kind failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_search
/// 
/// JNI Signature: TheStreamSearch(long handle, string, JString, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamSearch(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        query: string,
        kinds: JString,
        limit: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        let query: string = env.get_string(&query).expect("Failed to get query").into();
        let kinds: String = env.get_string(&kinds).expect("Failed to get kinds").into();
        // limit is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_search(query, kinds, limit))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_search failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_addBookmark
/// 
/// JNI Signature: TheStreamAddBookmark(long handle, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamAddBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        bookmarkId: number,
        seenAt: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // bookmarkId is already number
        // seenAt is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_add_bookmark(bookmarkId, seenAt))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_add_bookmark failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_addPost
/// 
/// JNI Signature: TheStreamAddPost(long handle, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamAddPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        postId: number,
        seenAt: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // postId is already number
        // seenAt is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_add_post(postId, seenAt))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_add_post failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_addMedia
/// 
/// JNI Signature: TheStreamAddMedia(long handle, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamAddMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        mediaId: number,
        seenAt: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // mediaId is already number
        // seenAt is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_add_media(mediaId, seenAt))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_add_media failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_addPerson
/// 
/// JNI Signature: TheStreamAddPerson(long handle, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamAddPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        personId: number,
        seenAt: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // personId is already number
        // seenAt is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_add_person(personId, seenAt))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_add_person failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// TheStreamMgmt.the_stream_addConversation
/// 
/// JNI Signature: TheStreamAddConversation(long handle, number, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeTheStreamAddConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        conversationId: number,
        seenAt: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // conversationId is already number
        // seenAt is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.stream_add_conversation(conversationId, seenAt))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] the_stream_add_conversation failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// ViewMgmt.view_addView
/// 
/// JNI Signature: ViewAddView(long handle, number, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeViewAddView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        index: number,
        badge: string,
        label: string
    
) -> () {
    
    // Convert JNI parameters to Rust types
        // index is already number
        let badge: string = env.get_string(&badge).expect("Failed to get badge").into();
        let label: string = env.get_string(&label).expect("Failed to get label").into();
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.add_view(index, badge, label))
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] view_add_view failed: {}", e);
            
        }
    }
}

/// ViewMgmt.view_getView
/// 
/// JNI Signature: ViewGetView(long handle, number) -> JString
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeViewGetView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> JString {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.get_view(id))
    }) {
        Ok(result) => {
            env.new_string(&result).expect("Failed to create Java string").into_raw()
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] view_get_view failed: {}", e);
            std::ptr::null_mut()
        }
    }
}

/// ViewMgmt.view_listViews
/// 
/// JNI Signature: ViewListViews(long handle) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeViewListViews(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    
) -> () {
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.list_views())
    }) {
        Ok(result) => {
            
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] view_list_views failed: {}", e);
            
        }
    }
}

/// ViewMgmt.view_deleteView
/// 
/// JNI Signature: ViewDeleteView(long handle, number) -> boolean
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RustExternalLayerRadicleService_nativeViewDeleteView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
        id: number
    
) -> boolean {
    
    // Convert JNI parameters to Rust types
        // id is already number
    
    
    
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        // Access service field with proper error handling
                let __service = service;
                Ok(__service.delete_view(id))
    }) {
        Ok(result) => {
            result as jboolean
        }
        Err(e) => {
            log::error!("[RustExternalLayerRadicleService] view_delete_view failed: {}", e);
            0
        }
    }
}

