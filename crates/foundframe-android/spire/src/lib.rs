// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/packages/spire-loom/machinery/bobbin/android/jni_bridge.jni.rs.mejs
// Template file: jni_bridge.jni.rs.mejs
// To override: Copy this template to loom/bobbin/android/jni_bridge.jni.rs.mejs
// 
// To modify the generated output, edit the template file above.
/**
 * JNI Bridge for RadicleService
 *
 * This module provides the JNI bindings between the Android Kotlin service
 * and the Rust core library.
 *
 * Architecture:
 * - Kotlin holds an opaque handle (jlong) to the Rust service
 * - Each JNI call locks the service, executes, then unlocks
 * - The handle pattern ensures thread-safe access to the core
 *
 * Generated for core: foundframe
 */



use jni::objects::{JClass, JString};
use jni::signature::JavaType;
use jni::sys::{jint, jlong, jboolean};
use jni::JavaVM;
use jni::JNIEnv;
use log::info;
use std::ffi::c_void;
use std::sync::{Arc, Mutex, OnceLock};
use foundframe as core;

// Type alias for the service handle used by JNI
// The core crate's main type is assumed to be Foundframe
pub type ServiceHandle = core::Foundframe;

/// Global service handle storage
/// 
/// Pattern: OnceLock<Arc<Mutex<T>>>
/// - OnceLock: Thread-safe one-time initialization
/// - Arc: Shared ownership across JNI calls  
/// - Mutex: Exclusive access during operations
/// 
/// The handle is initialized by nativeStartService and used by all
/// subsequent native method calls.
static SERVICE_HANDLE: OnceLock<Arc<Mutex<core::ServiceHandle>>> = OnceLock::new();

/// JNI registration function called when the native library is loaded
#[no_mangle]
pub extern "C" fn JNI_OnLoad(_vm: JavaVM, _reserved: c_void) -> jint {
    android_logger::init_once(
        android_logger::Config::default()
            .with_max_level(log::LevelFilter::Debug),
    );
    jni::sys::JNI_VERSION_1_6
}

/// Start the native service
/// 
/// Called from Kotlin: nativeStartService(homeDir, alias)
/// Returns: handle ID (always 1 for now, or -1 on error)
/// 
/// This initializes the core service and stores the handle globally.
/// The returned handle ID is passed back to all subsequent native calls.
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeStartService(
    mut env: JNIEnv,
    _class: JClass,
    home_dir: JString,
    alias: JString,
) -> jlong {
    let home_dir: String = env
        .get_string(&home_dir)
        .expect("Failed to get home_dir")
        .into();
    
    let alias: String = env
        .get_string(&alias)
        .expect("Failed to get alias")
        .into();
    
    info!("[RadicleService] Starting native service: home_dir={}, alias={}", home_dir, alias);
    
    match initialize_service(&home_dir, &alias) {
        Ok(handle) => {
            let _ = SERVICE_HANDLE.set(Arc::new(Mutex::new(handle)));
            info!("[RadicleService] Native service started successfully");
            1
        }
        Err(e) => {
            log::error!("[RadicleService] Failed to start native service: {}", e);
            -1
        }
    }
}

/// Stop the native service
/// 
/// Called from Kotlin when the service is being destroyed.
/// This drops the service handle, cleaning up resources.
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeStopService(
    _env: JNIEnv,
    _class: JClass,
    _handle: jlong,
) {
    info!("[RadicleService] Stopping native service");
    
    // OnceLock can't be cleared, but we can drop the inner value
    // by taking the mutex guard and replacing with None
    if let Some(handle) = SERVICE_HANDLE.get() {
        if let Ok(mut guard) = handle.lock() {
            // The handle will be dropped when the guard is released
            info!("[RadicleService] Service handle dropped");
        }
    }
}

/// Initialize the core service
/// 
/// Creates the actual core::ServiceHandle by calling into the core crate.
fn initialize_service(
    home_dir: &str, 
    alias: &str
) -> Result<core::ServiceHandle, Box<dyn std::error::Error>> {
    info!("[RadicleService] Initializing core service");
    
    // TODO: Replace with actual core initialization
    // core::ServiceHandle::new(home_dir, alias)
    unimplemented!("Service initialization not yet implemented in core")
}

/// Execute an operation with the service handle locked
/// 
/// This is the core pattern: lock → execute → unlock
/// All command methods use this to ensure thread-safe access.
fn with_service<F, R>(f: F) -> Result<R, String>
where
    F: FnOnce(&mut core::ServiceHandle) -> R,
{
    let handle = SERVICE_HANDLE
        .get()
        .ok_or_else(|| "Service not initialized".to_string())?;
    
    let mut guard = handle
        .lock()
        .map_err(|_| "Failed to lock service handle".to_string())?;
    
    Ok(f(&mut *guard))
}

/// ADD_BOOKMARK
/// 
/// JNI Signature: AddBookmark(long handle, string, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (url: STRING, title: STRING, notes: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for url
        // TODO: param conversion for title
        // TODO: param conversion for notes
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_BOOKMARK(url, title, notes))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_BOOKMARK failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_BOOKMARK
/// 
/// JNI Signature: GetBookmark(long handle, number) -> BOOKMARK
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOKMARK {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_BOOKMARK(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_BOOKMARK failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_BOOKMARK_BY_URI
/// 
/// JNI Signature: GetBookmarkByUri(long handle, string) -> BOOKMARK
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetBookmarkByUri(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (uri: STRING)
) -> BOOKMARK {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for uri
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_BOOKMARK_BY_URI(uri))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_BOOKMARK_BY_URI failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_BOOKMARKS
/// 
/// JNI Signature: ListBookmarks(long handle, number, number, BookmarkFilter) -> VEC<_B_O_O_K_M_A_R_K>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListBookmarks(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: BOOKMARK_FILTER)
) -> VEC<_B_O_O_K_M_A_R_K> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_BOOKMARKS(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_BOOKMARKS failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_BOOKMARK
/// 
/// JNI Signature: UpdateBookmark(long handle, number, string, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdateBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, title: STRING, notes: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for title
        // TODO: param conversion for notes
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_BOOKMARK(id, title, notes))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_BOOKMARK failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_BOOKMARK
/// 
/// JNI Signature: DeleteBookmark(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeleteBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_BOOKMARK(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_BOOKMARK failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_CONVERSATION
/// 
/// JNI Signature: AddConversation(long handle, ConversationEntry[], Date, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (content: VEC<CONVERSATION_ENTRY>, capture_time: DATE, title: STRING, source_url: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for content
        // TODO: param conversion for captureTime
        // TODO: param conversion for title
        // TODO: param conversion for sourceUrl
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_CONVERSATION(content, captureTime, title, sourceUrl))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_CONVERSATION failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_CONVERSATION
/// 
/// JNI Signature: GetConversation(long handle, number) -> CONVERSATION
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> CONVERSATION {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_CONVERSATION(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_CONVERSATION failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_CONVERSATIONS
/// 
/// JNI Signature: ListConversations(long handle, number, number, ConversationFilter) -> VEC<_C_O_N_V_E_R_S_A_T_I_O_N>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListConversations(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: CONVERSATION_FILTER)
) -> VEC<_C_O_N_V_E_R_S_A_T_I_O_N> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_CONVERSATIONS(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_CONVERSATIONS failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_CONVERSATION
/// 
/// JNI Signature: UpdateConversation(long handle, number, string, ConversationEntry[]) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdateConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, title: STRING, content: VEC<CONVERSATION_ENTRY>)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for title
        // TODO: param conversion for content
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_CONVERSATION(id, title, content))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_CONVERSATION failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_CONVERSATION
/// 
/// JNI Signature: DeleteConversation(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeleteConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_CONVERSATION(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_CONVERSATION failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_PARTICIPANT
/// 
/// JNI Signature: AddParticipant(long handle, number, number, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddParticipant(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64, person_id: I64, role: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
        // TODO: param conversion for personId
        // TODO: param conversion for role
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_PARTICIPANT(conversationId, personId, role))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_PARTICIPANT failed: {}", e);
            // TODO: error value
        }
    }
}

/// REMOVE_PARTICIPANT
/// 
/// JNI Signature: RemoveParticipant(long handle, number, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeRemoveParticipant(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64, person_id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
        // TODO: param conversion for personId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.REMOVE_PARTICIPANT(conversationId, personId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] REMOVE_PARTICIPANT failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_PARTICIPANTS
/// 
/// JNI Signature: ListParticipants(long handle, number) -> VEC<_I64>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListParticipants(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64)
) -> VEC<_I64> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_PARTICIPANTS(conversationId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_PARTICIPANTS failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_CONVERSATION_MEDIA
/// 
/// JNI Signature: AddConversationMedia(long handle, number, number, Record<string, unknown>) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64, media_id: I64, context: RECORD<STRING, UNKNOWN>)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
        // TODO: param conversion for mediaId
        // TODO: param conversion for context
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_CONVERSATION_MEDIA(conversationId, mediaId, context))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_CONVERSATION_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// REMOVE_CONVERSATION_MEDIA
/// 
/// JNI Signature: RemoveConversationMedia(long handle, number, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeRemoveConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64, media_id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
        // TODO: param conversion for mediaId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.REMOVE_CONVERSATION_MEDIA(conversationId, mediaId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] REMOVE_CONVERSATION_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_CONVERSATION_MEDIA
/// 
/// JNI Signature: ListConversationMedia(long handle, number) -> VEC<_I64>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListConversationMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64)
) -> VEC<_I64> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_CONVERSATION_MEDIA(conversationId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_CONVERSATION_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// GENERATE_PAIRING_CODE
/// 
/// JNI Signature: GeneratePairingCode(long handle) -> STRING
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGeneratePairingCode(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> STRING {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GENERATE_PAIRING_CODE())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GENERATE_PAIRING_CODE failed: {}", e);
            // TODO: error value
        }
    }
}

/// CONFIRM_PAIRING
/// 
/// JNI Signature: ConfirmPairing(long handle, string, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeConfirmPairing(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (device_id: STRING, code: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for deviceId
        // TODO: param conversion for code
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.CONFIRM_PAIRING(deviceId, code))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] CONFIRM_PAIRING failed: {}", e);
            // TODO: error value
        }
    }
}

/// UNPAIR_DEVICE
/// 
/// JNI Signature: UnpairDevice(long handle, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUnpairDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (device_id: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for deviceId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UNPAIR_DEVICE(deviceId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UNPAIR_DEVICE failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_PAIRED_DEVICES
/// 
/// JNI Signature: ListPairedDevices(long handle) -> VEC<_S_T_R_I_N_G>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListPairedDevices(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> VEC<_S_T_R_I_N_G> {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_PAIRED_DEVICES())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_PAIRED_DEVICES failed: {}", e);
            // TODO: error value
        }
    }
}

/// FOLLOW_DEVICE
/// 
/// JNI Signature: FollowDevice(long handle, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeFollowDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (device_id: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for deviceId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.FOLLOW_DEVICE(deviceId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] FOLLOW_DEVICE failed: {}", e);
            // TODO: error value
        }
    }
}

/// UNFOLLOW_DEVICE
/// 
/// JNI Signature: UnfollowDevice(long handle, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUnfollowDevice(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (device_id: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for deviceId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UNFOLLOW_DEVICE(deviceId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UNFOLLOW_DEVICE failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_FOLLOWERS
/// 
/// JNI Signature: ListFollowers(long handle) -> VEC<_S_T_R_I_N_G>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListFollowers(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> VEC<_S_T_R_I_N_G> {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_FOLLOWERS())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_FOLLOWERS failed: {}", e);
            // TODO: error value
        }
    }
}

/// IS_FOLLOWING
/// 
/// JNI Signature: IsFollowing(long handle, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeIsFollowing(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (device_id: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for deviceId
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.IS_FOLLOWING(deviceId))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] IS_FOLLOWING failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_MEDIA
/// 
/// JNI Signature: GetMedia(long handle, number) -> MEDIA
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> MEDIA {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_MEDIA(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_MEDIA_BY_HASH
/// 
/// JNI Signature: GetMediaByHash(long handle, string) -> MEDIA
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetMediaByHash(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (content_hash: STRING)
) -> MEDIA {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for contentHash
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_MEDIA_BY_HASH(contentHash))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_MEDIA_BY_HASH failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_MEDIA
/// 
/// JNI Signature: ListMedia(long handle, number, number, MediaFilter) -> VEC<_M_E_D_I_A>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: MEDIA_FILTER)
) -> VEC<_M_E_D_I_A> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_MEDIA(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_MEDIA
/// 
/// JNI Signature: UpdateMedia(long handle, number, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdateMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, title: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for title
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_MEDIA(id, title))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_MEDIA
/// 
/// JNI Signature: DeleteMedia(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeleteMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_MEDIA(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_MEDIA_LINK
/// 
/// JNI Signature: AddMediaLink(long handle, string, string, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddMediaLink(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (uri: STRING, mime_type: STRING, title: STRING, directory: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for uri
        // TODO: param conversion for mimeType
        // TODO: param conversion for title
        // TODO: param conversion for directory
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_MEDIA_LINK(uri, mimeType, title, directory))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_MEDIA_LINK failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_MEDIA_FILE
/// 
/// JNI Signature: AddMediaFile(long handle, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddMediaFile(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (uri: STRING, title: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for uri
        // TODO: param conversion for title
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_MEDIA_FILE(uri, title))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_MEDIA_FILE failed: {}", e);
            // TODO: error value
        }
    }
}

/// REGISTER_SOURCE
/// 
/// JNI Signature: RegisterSource(long handle, string, MediaAdapterType, MediaSourceCapability[], Record<string, unknown>) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeRegisterSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (url: STRING, adapter_type: MEDIA_ADAPTER_TYPE, capabilities: VEC<MEDIA_SOURCE_CAPABILITY>, config: RECORD<STRING, UNKNOWN>)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for url
        // TODO: param conversion for adapterType
        // TODO: param conversion for capabilities
        // TODO: param conversion for config
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.REGISTER_SOURCE(url, adapterType, capabilities, config))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] REGISTER_SOURCE failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_SOURCE
/// 
/// JNI Signature: GetSource(long handle, number) -> MEDIA_SOURCE
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> MEDIA_SOURCE {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_SOURCE(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_SOURCE failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_SOURCES
/// 
/// JNI Signature: ListSources(long handle, boolean, MediaAdapterType) -> VEC<MEDIA_SOURCE>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListSources(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (active_only: BOOL, adapter_type: MEDIA_ADAPTER_TYPE)
) -> VEC<MEDIA_SOURCE> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for activeOnly
        // TODO: param conversion for adapterType
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_SOURCES(activeOnly, adapterType))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_SOURCES failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_SOURCE
/// 
/// JNI Signature: UpdateSource(long handle, number, Record<string, unknown>, boolean) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdateSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, config: RECORD<STRING, UNKNOWN>, is_active: BOOL)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for config
        // TODO: param conversion for isActive
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_SOURCE(id, config, isActive))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_SOURCE failed: {}", e);
            // TODO: error value
        }
    }
}

/// UNREGISTER_SOURCE
/// 
/// JNI Signature: UnregisterSource(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUnregisterSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UNREGISTER_SOURCE(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UNREGISTER_SOURCE failed: {}", e);
            // TODO: error value
        }
    }
}

/// POLL_SOURCE
/// 
/// JNI Signature: PollSource(long handle, number) -> POLL_RESULT
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativePollSource(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> POLL_RESULT {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.POLL_SOURCE(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] POLL_SOURCE failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_HEALTH
/// 
/// JNI Signature: GetHealth(long handle, number) -> MEDIA_SOURCE_HEALTH
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetHealth(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> MEDIA_SOURCE_HEALTH {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_HEALTH(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_HEALTH failed: {}", e);
            // TODO: error value
        }
    }
}

/// RESET_ERRORS
/// 
/// JNI Signature: ResetErrors(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeResetErrors(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.RESET_ERRORS(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] RESET_ERRORS failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_NODE_ID
/// 
/// JNI Signature: GetNodeId(long handle) -> STRING
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetNodeId(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> STRING {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_NODE_ID())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_NODE_ID failed: {}", e);
            // TODO: error value
        }
    }
}

/// IS_NODE_RUNNING
/// 
/// JNI Signature: IsNodeRunning(long handle) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeIsNodeRunning(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> BOOL {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.IS_NODE_RUNNING())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] IS_NODE_RUNNING failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_NODE_ALIAS
/// 
/// JNI Signature: GetNodeAlias(long handle) -> STRING
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetNodeAlias(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> STRING {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_NODE_ALIAS())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_NODE_ALIAS failed: {}", e);
            // TODO: error value
        }
    }
}

/// START_NODE
/// 
/// JNI Signature: StartNode(long handle, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeStartNode(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (alias: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for alias
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.START_NODE(alias))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] START_NODE failed: {}", e);
            // TODO: error value
        }
    }
}

/// STOP_NODE
/// 
/// JNI Signature: StopNode(long handle) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeStopNode(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> () {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.STOP_NODE())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] STOP_NODE failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_PERSON
/// 
/// JNI Signature: AddPerson(long handle, string, string, Record<string, unknown>) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (display_name: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for displayName
        // TODO: param conversion for handle
        // TODO: param conversion for metadata
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_PERSON(displayName, handle, metadata))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_PERSON failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_PERSON
/// 
/// JNI Signature: GetPerson(long handle, number) -> PERSON
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> PERSON {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_PERSON(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_PERSON failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_PERSON_BY_HANDLE
/// 
/// JNI Signature: GetPersonByHandle(long handle, string) -> PERSON
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetPersonByHandle(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (handle: STRING)
) -> PERSON {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for handle
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_PERSON_BY_HANDLE(handle))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_PERSON_BY_HANDLE failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_PEOPLE
/// 
/// JNI Signature: ListPeople(long handle, number, number, PersonFilter) -> VEC<_P_E_R_S_O_N>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListPeople(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: PERSON_FILTER)
) -> VEC<_P_E_R_S_O_N> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_PEOPLE(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_PEOPLE failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_PERSON
/// 
/// JNI Signature: UpdatePerson(long handle, number, string, string, Record<string, unknown>) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdatePerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, display_name: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for displayName
        // TODO: param conversion for handle
        // TODO: param conversion for metadata
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_PERSON(id, displayName, handle, metadata))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_PERSON failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_PERSON
/// 
/// JNI Signature: DeletePerson(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeletePerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_PERSON(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_PERSON failed: {}", e);
            // TODO: error value
        }
    }
}

/// CREATE_REPOSITORY
/// 
/// JNI Signature: CreateRepository(long handle, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeCreateRepository(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (name: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for name
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.CREATE_REPOSITORY(name))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] CREATE_REPOSITORY failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_REPOSITORIES
/// 
/// JNI Signature: ListRepositories(long handle) -> VEC<_S_T_R_I_N_G>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListRepositories(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> VEC<_S_T_R_I_N_G> {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_REPOSITORIES())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_REPOSITORIES failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_DEFAULT_REPOSITORY
/// 
/// JNI Signature: GetDefaultRepository(long handle) -> STRING
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetDefaultRepository(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> STRING {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_DEFAULT_REPOSITORY())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_DEFAULT_REPOSITORY failed: {}", e);
            // TODO: error value
        }
    }
}

/// SET_DEFAULT_REPOSITORY
/// 
/// JNI Signature: SetDefaultRepository(long handle, string) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeSetDefaultRepository(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (name: STRING)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for name
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.SET_DEFAULT_REPOSITORY(name))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] SET_DEFAULT_REPOSITORY failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_POST
/// 
/// JNI Signature: AddPost(long handle, AccumulableBit[]) -> I64
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (bits: VEC<ACCUMULABLE_BIT>)
) -> I64 {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for bits
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_POST(bits))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_POST failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_POST
/// 
/// JNI Signature: GetPost(long handle, number) -> POST
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> POST {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_POST(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_POST failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_POSTS
/// 
/// JNI Signature: ListPosts(long handle, number, number, PostFilter) -> VEC<_P_O_S_T>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListPosts(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: POST_FILTER)
) -> VEC<_P_O_S_T> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_POSTS(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_POSTS failed: {}", e);
            // TODO: error value
        }
    }
}

/// UPDATE_POST
/// 
/// JNI Signature: UpdatePost(long handle, number, AccumulableBit[]) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeUpdatePost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64, bits: VEC<ACCUMULABLE_BIT>)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
        // TODO: param conversion for bits
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.UPDATE_POST(id, bits))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] UPDATE_POST failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_POST
/// 
/// JNI Signature: DeletePost(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeletePost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_POST(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_POST failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_ENTRIES
/// 
/// JNI Signature: GetEntries(long handle, number, number, TheStreamEntryFilter) -> VEC<THE_STREAM_ENTRY>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetEntries(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (limit: I64, offset: I64, filter: THE_STREAM_ENTRY_FILTER)
) -> VEC<THE_STREAM_ENTRY> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for limit
        // TODO: param conversion for offset
        // TODO: param conversion for filter
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_ENTRIES(limit, offset, filter))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_ENTRIES failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_ENTRY
/// 
/// JNI Signature: GetEntry(long handle, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetEntry(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_ENTRY(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_ENTRY failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_ENTRY
/// 
/// JNI Signature: DeleteEntry(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeleteEntry(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_ENTRY(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_ENTRY failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_ENTRIES_BY_KIND
/// 
/// JNI Signature: GetEntriesByKind(long handle, 'media' | 'post' | 'bookmark' | 'person' | 'conversation', number, number) -> VEC<THE_STREAM_ENTRY>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetEntriesByKind(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (kind: 'MEDIA' | 'POST' | 'BOOKMARK' | 'PERSON' | 'CONVERSATION', limit: I64, before: I64)
) -> VEC<THE_STREAM_ENTRY> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for kind
        // TODO: param conversion for limit
        // TODO: param conversion for before
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_ENTRIES_BY_KIND(kind, limit, before))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_ENTRIES_BY_KIND failed: {}", e);
            // TODO: error value
        }
    }
}

/// SEARCH
/// 
/// JNI Signature: Search(long handle, string, string[], number) -> VEC<THE_STREAM_ENTRY>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeSearch(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (query: STRING, kinds: VEC<_S_T_R_I_N_G>, limit: I64)
) -> VEC<THE_STREAM_ENTRY> {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for query
        // TODO: param conversion for kinds
        // TODO: param conversion for limit
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.SEARCH(query, kinds, limit))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] SEARCH failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_BOOKMARK
/// 
/// JNI Signature: AddBookmark(long handle, number, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddBookmark(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (bookmark_id: I64, seen_at: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for bookmarkId
        // TODO: param conversion for seenAt
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_BOOKMARK(bookmarkId, seenAt))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_BOOKMARK failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_POST
/// 
/// JNI Signature: AddPost(long handle, number, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddPost(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (post_id: I64, seen_at: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for postId
        // TODO: param conversion for seenAt
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_POST(postId, seenAt))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_POST failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_MEDIA
/// 
/// JNI Signature: AddMedia(long handle, number, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddMedia(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (media_id: I64, seen_at: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for mediaId
        // TODO: param conversion for seenAt
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_MEDIA(mediaId, seenAt))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_MEDIA failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_PERSON
/// 
/// JNI Signature: AddPerson(long handle, number, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddPerson(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (person_id: I64, seen_at: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for personId
        // TODO: param conversion for seenAt
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_PERSON(personId, seenAt))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_PERSON failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_CONVERSATION
/// 
/// JNI Signature: AddConversation(long handle, number, number) -> THE_STREAM_ENTRY
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddConversation(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (conversation_id: I64, seen_at: I64)
) -> THE_STREAM_ENTRY {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for conversationId
        // TODO: param conversion for seenAt
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_CONVERSATION(conversationId, seenAt))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_CONVERSATION failed: {}", e);
            // TODO: error value
        }
    }
}

/// ADD_VIEW
/// 
/// JNI Signature: AddView(long handle, number, string, string) -> ()
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeAddView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (index: I64, badge: STRING, label: STRING)
) -> () {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for index
        // TODO: param conversion for badge
        // TODO: param conversion for label
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.ADD_VIEW(index, badge, label))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] ADD_VIEW failed: {}", e);
            // TODO: error value
        }
    }
}

/// GET_VIEW
/// 
/// JNI Signature: GetView(long handle, number) -> VIEW
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeGetView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> VIEW {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.GET_VIEW(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] GET_VIEW failed: {}", e);
            // TODO: error value
        }
    }
}

/// LIST_VIEWS
/// 
/// JNI Signature: ListViews(long handle) -> VEC<()>
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeListViews(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    ()
) -> VEC<()> {
        
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.LIST_VIEWS())
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] LIST_VIEWS failed: {}", e);
            // TODO: error value
        }
    }
}

/// DELETE_VIEW
/// 
/// JNI Signature: DeleteView(long handle, number) -> BOOL
/// 
/// This function:
/// 1. Locks the service handle (Mutex::lock)
/// 2. Calls the core method on the handle
/// 3. Converts the result to JNI types
/// 4. Unlocks the handle (when guard drops)
#[no_mangle]
pub extern "C" fn Java_ty_circulari_o19_service_RadicleService_nativeDeleteView(
    mut env: JNIEnv,
    _class: JClass,
    _handle: jlong,
    (id: I64)
) -> BOOL {
        // Convert JNI parameters to Rust types
        // TODO: param conversion for id
            
    // Lock handle → execute → unlock
    match with_service(|service| -> Result<_, String> {
        Ok(service.DELETE_VIEW(id))
    }) {
        Ok(result) => {
            // TODO: return conversion
            result
        }
        Err(e) => {
            log::error!("[RadicleService] DELETE_VIEW failed: {}", e);
            // TODO: error value
        }
    }
}

