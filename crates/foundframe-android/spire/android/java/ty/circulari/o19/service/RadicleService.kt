// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/packages/spire-loom/machinery/bobbin/android/service.kt.mejs
// Template file: service.kt.mejs
// To override: Copy this template to loom/bobbin/android/service.kt.mejs
// 
// To modify the generated output, edit the template file above.
package ty.circulari.o19.service



import android.app.Notification
import android.app.NotificationChannel
import android.app.NotificationManager
import android.app.Service
import android.content.Context
import android.content.Intent
import android.os.Build
import android.os.IBinder
import android.os.Process
import android.os.RemoteException
import android.util.Log
import ty.circulari.o19.IRadicleService

/**
 * RadicleService - Generated foreground service with AIDL IPC
 *
 * This service runs in a separate process (:foundframe) and provides cross-process
 * access to the Rust core library via AIDL.
 *
 * Architecture:
 * - Started service: Maintains native Rust core lifecycle via JNI
 * - Bound service: Provides AIDL interface for IPC from other apps/processes
 * - The service is both started (for lifecycle) AND bound (for IPC)
 */
class RadicleService : Service() {

    companion object {
        private const val TAG = "foundframe.radicle"
        private const val NOTIFICATION_ID = 1
        private const val CHANNEL_ID = "foundframe.radicle.channel"

        init {
            try {
                Log.i(TAG, "[RadicleService] Loading native library...")
                System.loadLibrary("android")
                Log.i(TAG, "[RadicleService] Native library loaded successfully")
            } catch (e: Throwable) {
                Log.e(TAG, "[RadicleService] FAILED to load native library: ${e.message}", e)
            }
        }
    }

    /**
     * Native handle to the Rust service instance.
     * This is an opaque pointer - the actual Rust object lives in native memory.
     */
    private var nativeHandle: Long = 0

    /** Start the native service - returns a handle to the service instance */
    private external fun nativeStartService(homeDir: String, alias: String): Long

    /** Stop the native service and release the handle */
    private external fun nativeStopService(handle: Long)

        /** ADD_BOOKMARK */
    private external fun nativeAddBookmark(
        handle: Long, (url: STRING, title: STRING, notes: STRING)    ): [object Object]

        /** GET_BOOKMARK */
    private external fun nativeGetBookmark(
        handle: Long, (id: INT)    ): BOOKMARK

        /** GET_BOOKMARK_BY_URI */
    private external fun nativeGetBookmarkByUri(
        handle: Long, (uri: STRING)    ): BOOKMARK

        /** LIST_BOOKMARKS */
    private external fun nativeListBookmarks(
        handle: Long, (limit: INT, offset: INT, filter: BOOKMARK_FILTER)    ): LIST<_B_O_O_K_M_A_R_K>

        /** UPDATE_BOOKMARK */
    private external fun nativeUpdateBookmark(
        handle: Long, (id: INT, title: STRING, notes: STRING)    ): [object Object]

        /** DELETE_BOOKMARK */
    private external fun nativeDeleteBookmark(
        handle: Long, (id: INT)    ): [object Object]

        /** ADD_CONVERSATION */
    private external fun nativeAddConversation(
        handle: Long, (content: LIST<CONVERSATION_ENTRY>, captureTime: DATE, title: STRING, sourceUrl: STRING)    ): [object Object]

        /** GET_CONVERSATION */
    private external fun nativeGetConversation(
        handle: Long, (id: INT)    ): CONVERSATION

        /** LIST_CONVERSATIONS */
    private external fun nativeListConversations(
        handle: Long, (limit: INT, offset: INT, filter: CONVERSATION_FILTER)    ): LIST<_C_O_N_V_E_R_S_A_T_I_O_N>

        /** UPDATE_CONVERSATION */
    private external fun nativeUpdateConversation(
        handle: Long, (id: INT, title: STRING, content: LIST<CONVERSATION_ENTRY>)    ): [object Object]

        /** DELETE_CONVERSATION */
    private external fun nativeDeleteConversation(
        handle: Long, (id: INT)    ): [object Object]

        /** ADD_PARTICIPANT */
    private external fun nativeAddParticipant(
        handle: Long, (conversationId: INT, personId: INT, role: STRING)    ): [object Object]

        /** REMOVE_PARTICIPANT */
    private external fun nativeRemoveParticipant(
        handle: Long, (conversationId: INT, personId: INT)    ): [object Object]

        /** LIST_PARTICIPANTS */
    private external fun nativeListParticipants(
        handle: Long, (conversationId: INT)    ): LIST<UNDEFINED>

        /** ADD_CONVERSATION_MEDIA */
    private external fun nativeAddConversationMedia(
        handle: Long, (conversationId: INT, mediaId: INT, context: RECORD<STRING, UNKNOWN>)    ): [object Object]

        /** REMOVE_CONVERSATION_MEDIA */
    private external fun nativeRemoveConversationMedia(
        handle: Long, (conversationId: INT, mediaId: INT)    ): [object Object]

        /** LIST_CONVERSATION_MEDIA */
    private external fun nativeListConversationMedia(
        handle: Long, (conversationId: INT)    ): LIST<UNDEFINED>

        /** GENERATE_PAIRING_CODE */
    private external fun nativeGeneratePairingCode(
        handle: Long    ): [object Object]

        /** CONFIRM_PAIRING */
    private external fun nativeConfirmPairing(
        handle: Long, (deviceId: STRING, code: STRING)    ): [object Object]

        /** UNPAIR_DEVICE */
    private external fun nativeUnpairDevice(
        handle: Long, (deviceId: STRING)    ): [object Object]

        /** LIST_PAIRED_DEVICES */
    private external fun nativeListPairedDevices(
        handle: Long    ): LIST<UNDEFINED>

        /** FOLLOW_DEVICE */
    private external fun nativeFollowDevice(
        handle: Long, (deviceId: STRING)    ): [object Object]

        /** UNFOLLOW_DEVICE */
    private external fun nativeUnfollowDevice(
        handle: Long, (deviceId: STRING)    ): [object Object]

        /** LIST_FOLLOWERS */
    private external fun nativeListFollowers(
        handle: Long    ): LIST<UNDEFINED>

        /** IS_FOLLOWING */
    private external fun nativeIsFollowing(
        handle: Long, (deviceId: STRING)    ): [object Object]

        /** GET_MEDIA */
    private external fun nativeGetMedia(
        handle: Long, (id: INT)    ): MEDIA

        /** GET_MEDIA_BY_HASH */
    private external fun nativeGetMediaByHash(
        handle: Long, (contentHash: STRING)    ): MEDIA

        /** LIST_MEDIA */
    private external fun nativeListMedia(
        handle: Long, (limit: INT, offset: INT, filter: MEDIA_FILTER)    ): LIST<_M_E_D_I_A>

        /** UPDATE_MEDIA */
    private external fun nativeUpdateMedia(
        handle: Long, (id: INT, title: STRING)    ): [object Object]

        /** DELETE_MEDIA */
    private external fun nativeDeleteMedia(
        handle: Long, (id: INT)    ): [object Object]

        /** ADD_MEDIA_LINK */
    private external fun nativeAddMediaLink(
        handle: Long, (uri: STRING, mimeType: STRING, title: STRING, directory: STRING)    ): [object Object]

        /** ADD_MEDIA_FILE */
    private external fun nativeAddMediaFile(
        handle: Long, (uri: STRING, title: STRING)    ): [object Object]

        /** REGISTER_SOURCE */
    private external fun nativeRegisterSource(
        handle: Long, (url: STRING, adapterType: MEDIA_ADAPTER_TYPE, capabilities: LIST<MEDIA_SOURCE_CAPABILITY>, config: RECORD<STRING, UNKNOWN>)    ): [object Object]

        /** GET_SOURCE */
    private external fun nativeGetSource(
        handle: Long, (id: INT)    ): MEDIA_SOURCE

        /** LIST_SOURCES */
    private external fun nativeListSources(
        handle: Long, (activeOnly: BOOLEAN, adapterType: MEDIA_ADAPTER_TYPE)    ): LIST<MEDIA_SOURCE>

        /** UPDATE_SOURCE */
    private external fun nativeUpdateSource(
        handle: Long, (id: INT, config: RECORD<STRING, UNKNOWN>, isActive: BOOLEAN)    ): [object Object]

        /** UNREGISTER_SOURCE */
    private external fun nativeUnregisterSource(
        handle: Long, (id: INT)    ): [object Object]

        /** POLL_SOURCE */
    private external fun nativePollSource(
        handle: Long, (id: INT)    ): POLL_RESULT

        /** GET_HEALTH */
    private external fun nativeGetHealth(
        handle: Long, (id: INT)    ): MEDIA_SOURCE_HEALTH

        /** RESET_ERRORS */
    private external fun nativeResetErrors(
        handle: Long, (id: INT)    ): [object Object]

        /** GET_NODE_ID */
    private external fun nativeGetNodeId(
        handle: Long    ): [object Object]

        /** IS_NODE_RUNNING */
    private external fun nativeIsNodeRunning(
        handle: Long    ): [object Object]

        /** GET_NODE_ALIAS */
    private external fun nativeGetNodeAlias(
        handle: Long    ): [object Object]

        /** START_NODE */
    private external fun nativeStartNode(
        handle: Long, (alias: STRING)    ): [object Object]

        /** STOP_NODE */
    private external fun nativeStopNode(
        handle: Long    ): [object Object]

        /** ADD_PERSON */
    private external fun nativeAddPerson(
        handle: Long, (displayName: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)    ): [object Object]

        /** GET_PERSON */
    private external fun nativeGetPerson(
        handle: Long, (id: INT)    ): PERSON

        /** GET_PERSON_BY_HANDLE */
    private external fun nativeGetPersonByHandle(
        handle: Long, (handle: STRING)    ): PERSON

        /** LIST_PEOPLE */
    private external fun nativeListPeople(
        handle: Long, (limit: INT, offset: INT, filter: PERSON_FILTER)    ): LIST<_P_E_R_S_O_N>

        /** UPDATE_PERSON */
    private external fun nativeUpdatePerson(
        handle: Long, (id: INT, displayName: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)    ): [object Object]

        /** DELETE_PERSON */
    private external fun nativeDeletePerson(
        handle: Long, (id: INT)    ): [object Object]

        /** CREATE_REPOSITORY */
    private external fun nativeCreateRepository(
        handle: Long, (name: STRING)    ): [object Object]

        /** LIST_REPOSITORIES */
    private external fun nativeListRepositories(
        handle: Long    ): LIST<UNDEFINED>

        /** GET_DEFAULT_REPOSITORY */
    private external fun nativeGetDefaultRepository(
        handle: Long    ): [object Object]

        /** SET_DEFAULT_REPOSITORY */
    private external fun nativeSetDefaultRepository(
        handle: Long, (name: STRING)    ): [object Object]

        /** ADD_POST */
    private external fun nativeAddPost(
        handle: Long, (bits: LIST<ACCUMULABLE_BIT>)    ): [object Object]

        /** GET_POST */
    private external fun nativeGetPost(
        handle: Long, (id: INT)    ): POST

        /** LIST_POSTS */
    private external fun nativeListPosts(
        handle: Long, (limit: INT, offset: INT, filter: POST_FILTER)    ): LIST<_P_O_S_T>

        /** UPDATE_POST */
    private external fun nativeUpdatePost(
        handle: Long, (id: INT, bits: LIST<ACCUMULABLE_BIT>)    ): [object Object]

        /** DELETE_POST */
    private external fun nativeDeletePost(
        handle: Long, (id: INT)    ): [object Object]

        /** GET_ENTRIES */
    private external fun nativeGetEntries(
        handle: Long, (limit: INT, offset: INT, filter: THE_STREAM_ENTRY_FILTER)    ): LIST<THE_STREAM_ENTRY>

        /** GET_ENTRY */
    private external fun nativeGetEntry(
        handle: Long, (id: INT)    ): THE_STREAM_ENTRY

        /** DELETE_ENTRY */
    private external fun nativeDeleteEntry(
        handle: Long, (id: INT)    ): [object Object]

        /** GET_ENTRIES_BY_KIND */
    private external fun nativeGetEntriesByKind(
        handle: Long, (kind: 'MEDIA' | 'POST' | 'BOOKMARK' | 'PERSON' | 'CONVERSATION', limit: INT, before: INT)    ): LIST<THE_STREAM_ENTRY>

        /** SEARCH */
    private external fun nativeSearch(
        handle: Long, (query: STRING, kinds: LIST<UNDEFINED>, limit: INT)    ): LIST<THE_STREAM_ENTRY>

        /** ADD_BOOKMARK */
    private external fun nativeAddBookmark(
        handle: Long, (bookmarkId: INT, seenAt: INT)    ): THE_STREAM_ENTRY

        /** ADD_POST */
    private external fun nativeAddPost(
        handle: Long, (postId: INT, seenAt: INT)    ): THE_STREAM_ENTRY

        /** ADD_MEDIA */
    private external fun nativeAddMedia(
        handle: Long, (mediaId: INT, seenAt: INT)    ): THE_STREAM_ENTRY

        /** ADD_PERSON */
    private external fun nativeAddPerson(
        handle: Long, (personId: INT, seenAt: INT)    ): THE_STREAM_ENTRY

        /** ADD_CONVERSATION */
    private external fun nativeAddConversation(
        handle: Long, (conversationId: INT, seenAt: INT)    ): THE_STREAM_ENTRY

        /** ADD_VIEW */
    private external fun nativeAddView(
        handle: Long, (index: INT, badge: STRING, label: STRING)    ): [object Object]

        /** GET_VIEW */
    private external fun nativeGetView(
        handle: Long, (id: INT)    ): VIEW

        /** LIST_VIEWS */
    private external fun nativeListViews(
        handle: Long    ): LIST<UNDEFINED>

        /** DELETE_VIEW */
    private external fun nativeDeleteView(
        handle: Long, (id: INT)    ): [object Object]


    /**
     * AIDL Binder Stub - generated by NDK aidl tool, implemented here.
     *
     * This inner class extends the AIDL-generated Stub to provide the actual
     * implementation. It bridges AIDL calls to JNI calls into the Rust core.
     *
     * Security: Access is controlled by AndroidManifest.xml permission:
     * ty.circulari.o19.ACCESS_FOUNDFRAME (signature level)
     */
    private val aidlBinder = object : IRadicleService.Stub() {
        
        @Throws(RemoteException::class)
        override  fun ADD_BOOKMARK(url: STRING, title: STRING, notes: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddBookmark(nativeHandle, url, title, notes)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_BOOKMARK(id: INT): BOOKMARK {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetBookmark(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_BOOKMARK_BY_URI(uri: STRING): BOOKMARK {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetBookmarkByUri(nativeHandle, uri)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_BOOKMARKS(limit: INT, offset: INT, filter: BOOKMARK_FILTER): LIST<_B_O_O_K_M_A_R_K> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListBookmarks(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_BOOKMARK(id: INT, title: STRING, notes: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdateBookmark(nativeHandle, id, title, notes)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_BOOKMARK(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeleteBookmark(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_CONVERSATION(content: LIST<CONVERSATION_ENTRY>, captureTime: DATE, title: STRING, sourceUrl: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddConversation(nativeHandle, content, captureTime, title, sourceUrl)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_CONVERSATION(id: INT): CONVERSATION {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetConversation(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_CONVERSATIONS(limit: INT, offset: INT, filter: CONVERSATION_FILTER): LIST<_C_O_N_V_E_R_S_A_T_I_O_N> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListConversations(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_CONVERSATION(id: INT, title: STRING, content: LIST<CONVERSATION_ENTRY>): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdateConversation(nativeHandle, id, title, content)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_CONVERSATION(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeleteConversation(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_PARTICIPANT(conversationId: INT, personId: INT, role: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddParticipant(nativeHandle, conversationId, personId, role)
        }
        
        @Throws(RemoteException::class)
        override  fun REMOVE_PARTICIPANT(conversationId: INT, personId: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeRemoveParticipant(nativeHandle, conversationId, personId)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_PARTICIPANTS(conversationId: INT): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListParticipants(nativeHandle, conversationId)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_CONVERSATION_MEDIA(conversationId: INT, mediaId: INT, context: RECORD<STRING, UNKNOWN>): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddConversationMedia(nativeHandle, conversationId, mediaId, context)
        }
        
        @Throws(RemoteException::class)
        override  fun REMOVE_CONVERSATION_MEDIA(conversationId: INT, mediaId: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeRemoveConversationMedia(nativeHandle, conversationId, mediaId)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_CONVERSATION_MEDIA(conversationId: INT): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListConversationMedia(nativeHandle, conversationId)
        }
        
        @Throws(RemoteException::class)
        override  fun GENERATE_PAIRING_CODE(): STRING {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGeneratePairingCode(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun CONFIRM_PAIRING(deviceId: STRING, code: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeConfirmPairing(nativeHandle, deviceId, code)
        }
        
        @Throws(RemoteException::class)
        override  fun UNPAIR_DEVICE(deviceId: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUnpairDevice(nativeHandle, deviceId)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_PAIRED_DEVICES(): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListPairedDevices(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun FOLLOW_DEVICE(deviceId: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeFollowDevice(nativeHandle, deviceId)
        }
        
        @Throws(RemoteException::class)
        override  fun UNFOLLOW_DEVICE(deviceId: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUnfollowDevice(nativeHandle, deviceId)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_FOLLOWERS(): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListFollowers(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun IS_FOLLOWING(deviceId: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeIsFollowing(nativeHandle, deviceId)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_MEDIA(id: INT): MEDIA {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetMedia(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_MEDIA_BY_HASH(contentHash: STRING): MEDIA {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetMediaByHash(nativeHandle, contentHash)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_MEDIA(limit: INT, offset: INT, filter: MEDIA_FILTER): LIST<_M_E_D_I_A> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListMedia(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_MEDIA(id: INT, title: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdateMedia(nativeHandle, id, title)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_MEDIA(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeleteMedia(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_MEDIA_LINK(uri: STRING, mimeType: STRING, title: STRING, directory: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddMediaLink(nativeHandle, uri, mimeType, title, directory)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_MEDIA_FILE(uri: STRING, title: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddMediaFile(nativeHandle, uri, title)
        }
        
        @Throws(RemoteException::class)
        override  fun REGISTER_SOURCE(url: STRING, adapterType: MEDIA_ADAPTER_TYPE, capabilities: LIST<MEDIA_SOURCE_CAPABILITY>, config: RECORD<STRING, UNKNOWN>): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeRegisterSource(nativeHandle, url, adapterType, capabilities, config)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_SOURCE(id: INT): MEDIA_SOURCE {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetSource(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_SOURCES(activeOnly: BOOLEAN, adapterType: MEDIA_ADAPTER_TYPE): LIST<MEDIA_SOURCE> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListSources(nativeHandle, activeOnly, adapterType)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_SOURCE(id: INT, config: RECORD<STRING, UNKNOWN>, isActive: BOOLEAN): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdateSource(nativeHandle, id, config, isActive)
        }
        
        @Throws(RemoteException::class)
        override  fun UNREGISTER_SOURCE(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUnregisterSource(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun POLL_SOURCE(id: INT): POLL_RESULT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativePollSource(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_HEALTH(id: INT): MEDIA_SOURCE_HEALTH {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetHealth(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun RESET_ERRORS(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeResetErrors(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_NODE_ID(): STRING {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetNodeId(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun IS_NODE_RUNNING(): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeIsNodeRunning(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_NODE_ALIAS(): STRING {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetNodeAlias(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun START_NODE(alias: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeStartNode(nativeHandle, alias)
        }
        
        @Throws(RemoteException::class)
        override  fun STOP_NODE(): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeStopNode(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_PERSON(displayName: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddPerson(nativeHandle, displayName, handle, metadata)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_PERSON(id: INT): PERSON {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetPerson(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_PERSON_BY_HANDLE(handle: STRING): PERSON {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetPersonByHandle(nativeHandle, handle)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_PEOPLE(limit: INT, offset: INT, filter: PERSON_FILTER): LIST<_P_E_R_S_O_N> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListPeople(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_PERSON(id: INT, displayName: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdatePerson(nativeHandle, id, displayName, handle, metadata)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_PERSON(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeletePerson(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun CREATE_REPOSITORY(name: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeCreateRepository(nativeHandle, name)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_REPOSITORIES(): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListRepositories(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_DEFAULT_REPOSITORY(): STRING {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetDefaultRepository(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun SET_DEFAULT_REPOSITORY(name: STRING): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeSetDefaultRepository(nativeHandle, name)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_POST(bits: LIST<ACCUMULABLE_BIT>): INT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddPost(nativeHandle, bits)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_POST(id: INT): POST {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetPost(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_POSTS(limit: INT, offset: INT, filter: POST_FILTER): LIST<_P_O_S_T> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListPosts(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun UPDATE_POST(id: INT, bits: LIST<ACCUMULABLE_BIT>): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeUpdatePost(nativeHandle, id, bits)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_POST(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeletePost(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_ENTRIES(limit: INT, offset: INT, filter: THE_STREAM_ENTRY_FILTER): LIST<THE_STREAM_ENTRY> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetEntries(nativeHandle, limit, offset, filter)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_ENTRY(id: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetEntry(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_ENTRY(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeleteEntry(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_ENTRIES_BY_KIND(kind: 'MEDIA' | 'POST' | 'BOOKMARK' | 'PERSON' | 'CONVERSATION', limit: INT, before: INT): LIST<THE_STREAM_ENTRY> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetEntriesByKind(nativeHandle, kind, limit, before)
        }
        
        @Throws(RemoteException::class)
        override  fun SEARCH(query: STRING, kinds: LIST<UNDEFINED>, limit: INT): LIST<THE_STREAM_ENTRY> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeSearch(nativeHandle, query, kinds, limit)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_BOOKMARK(bookmarkId: INT, seenAt: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddBookmark(nativeHandle, bookmarkId, seenAt)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_POST(postId: INT, seenAt: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddPost(nativeHandle, postId, seenAt)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_MEDIA(mediaId: INT, seenAt: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddMedia(nativeHandle, mediaId, seenAt)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_PERSON(personId: INT, seenAt: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddPerson(nativeHandle, personId, seenAt)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_CONVERSATION(conversationId: INT, seenAt: INT): THE_STREAM_ENTRY {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddConversation(nativeHandle, conversationId, seenAt)
        }
        
        @Throws(RemoteException::class)
        override  fun ADD_VIEW(index: INT, badge: STRING, label: STRING): UNIT {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeAddView(nativeHandle, index, badge, label)
        }
        
        @Throws(RemoteException::class)
        override  fun GET_VIEW(id: INT): VIEW {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeGetView(nativeHandle, id)
        }
        
        @Throws(RemoteException::class)
        override  fun LIST_VIEWS(): LIST<UNDEFINED> {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeListViews(nativeHandle)
        }
        
        @Throws(RemoteException::class)
        override  fun DELETE_VIEW(id: INT): BOOLEAN {
            check(nativeHandle != 0L) {
                "Service not initialized - ensure service is started before binding"
            }
            return nativeDeleteView(nativeHandle, id)
        }
            }

    override fun onCreate() {
        super.onCreate()
        Log.i(TAG, "[RadicleService] Service creating in pid ${Process.myPid()}")

        // Create notification channel for foreground service
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            val channel = NotificationChannel(
                CHANNEL_ID,
                "Radicle Service",
                NotificationManager.IMPORTANCE_LOW
            ).apply {
                description = "Background service for foundframe"
                setShowBadge(false)
            }

            val notificationManager = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
            notificationManager.createNotificationChannel(channel)
        }

        // Start as foreground service
        startForeground(NOTIFICATION_ID, createNotification())
        Log.i(TAG, "[RadicleService] Started as foreground service")
    }

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        Log.i(TAG, "[RadicleService] Service starting (startId=$startId)")

        val homeDir = getDir(".foundframe", Context.MODE_PRIVATE).absolutePath
        val alias = intent?.getStringExtra("alias") ?: "android"

        // Start the native service in a background thread
        Thread {
            try {
                nativeHandle = nativeStartService(homeDir, alias)
                if (nativeHandle < 0) {
                    Log.e(TAG, "[RadicleService] Native service failed to start")
                    stopSelf(startId)
                    return@Thread
                }

                Log.i(TAG, "[RadicleService] Native service started with handle=$nativeHandle")

                // Service keeps running until explicitly stopped
                // The Rust core runs its own event loop
                Thread.currentThread().join()

                Log.w(TAG, "[RadicleService] Native service thread exited unexpectedly")
            } catch (e: Exception) {
                Log.e(TAG, "[RadicleService] Native service crashed: ${e.message}", e)
            } finally {
                if (nativeHandle != 0L) {
                    nativeStopService(nativeHandle)
                }
                nativeHandle = 0
                stopSelf(startId)
            }
        }.apply {
            name = "RadicleService-Thread-$startId"
            start()
        }

        return START_STICKY
    }

    /**
     * Called when a client binds to this service.
     *
     * Returns the AIDL binder stub that implements IFoundframeRadicle interface.
     * This enables cross-process IPC for apps with the same signing key.
     *
     * @param intent The Intent that was used to bind to this service
     * @return IBinder The AIDL interface implementation
     */
    override fun onBind(intent: Intent): IBinder {
        Log.i(TAG, "[RadicleService] onBind() - returning AIDL binder")
        return aidlBinder
    }

    override fun onDestroy() {
        super.onDestroy()
        Log.i(TAG, "[RadicleService] Service destroying")
        if (nativeHandle != 0L) {
            nativeStopService(nativeHandle)
            nativeHandle = 0
        }
    }

    /**
     * Check if the native service is ready for AIDL calls.
     * Clients can use this to verify the service is initialized before calling methods.
     */
    fun isReady(): Boolean = nativeHandle != 0L

    private fun createNotification(): Notification {
        val builder = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            Notification.Builder(this, CHANNEL_ID)
        } else {
            @Suppress("DEPRECATION")
            Notification.Builder(this)
        }

        return builder
            .setContentTitle("foundframe Service")
            .setContentText("Running in background")
            .setSmallIcon(android.R.drawable.ic_menu_share)
            .setOngoing(true)
            .build()
    }
}
