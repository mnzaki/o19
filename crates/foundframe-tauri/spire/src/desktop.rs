// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/packages/spire-loom/machinery/bobbin/tauri/desktop.rs.mejs
// Template file: desktop.rs.mejs
// To override: Copy this template to loom/bobbin/tauri/desktop.rs.mejs
// 
// To modify the generated output, edit the template file above.
//! Desktop Platform Implementation - GENERATED by spire-loom
//!
//! On desktop, we initialize o19-foundframe directly and call into it.
//!
//! NOTE: This template generates foundframe-specific access patterns:
//! - thestream operations use with_thestream(|stream| ...)
//! - device_manager operations use with_device_manager(|dm| ...)



use crate::spire::platform::SpireFoundframePlatformTrait;
use crate::spire::error::{Error, Result};
use crate::spire::models::*;
use serde::de::DeserializeOwned;
use std::path::PathBuf;
use tauri::{AppHandle, Manager, Runtime, plugin::PluginApi};

// Import core traits and types needed for method implementations
use o19_foundframe::bookmark::BookmarkStream;
use o19_foundframe::media::MediaStream;
use o19_foundframe::person::PersonStream;
use o19_foundframe::post::PostStream;
use o19_foundframe::conversation::ConversationStream;
use o19_foundframe::device::{DeviceManager, NodeId};
use core::str::FromStr;

/// Desktop platform implementation.
///
/// Holds the foundframe runtime and provides direct access to all operations.
/// 
/// NOTE: This platform does NOT initialize o19-foundframe itself.
/// The Foundframe instance must be created by the caller and passed to 
/// `DesktopPlatform::from_foundframe()`.
pub struct DesktopPlatform<R: Runtime> {
  app_handle: AppHandle<R>,
  foundframe: std::sync::Mutex<Option<o19_foundframe::Foundframe>>,
}

impl<R: Runtime> DesktopPlatform<R> {
  /// Create a DesktopPlatform from an existing Foundframe instance.
  /// 
  /// The caller is responsible for initializing o19-foundframe and passing
  /// the resulting Foundframe instance. This allows the main plugin to control
  /// initialization options, callbacks, and lifecycle.
  pub fn from_foundframe(
    app_handle: AppHandle<R>,
    foundframe: o19_foundframe::Foundframe,
  ) -> Self {
    Self { 
      app_handle,
      foundframe: std::sync::Mutex::new(Some(foundframe)),
    }
  }

  pub fn app_handle(&self) -> &AppHandle<R> {
    &self.app_handle
  }

  /// Get a reference to the underlying Foundframe instance.
  pub fn foundframe(&self) -> std::sync::MutexGuard<Option<o19_foundframe::Foundframe>> {
    self.foundframe.lock().unwrap()
  }
}

fn default_pkb_path() -> PathBuf {
  PathBuf::from("pkb")
}

impl<R: Runtime> SpireFoundframePlatformTrait for DesktopPlatform<R> {
  fn exit(&self, code: i32) {
    self.app_handle.exit(code);
  }

  fn request_permissions(&self) -> Result<NotificationPermissionStatus> {
    Ok(NotificationPermissionStatus {
      status: "granted".into(),
    })
  }

  fn shutdown(&self) -> Result<()> {
    // TODO: Shutdown o19-foundframe
    Ok(())
  }

  fn ADD_BOOKMARK(
    &self,
    (url: STRING, title: STRING, notes: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_BOOKMARK: stub implementation");
    ()
  }

  fn GET_BOOKMARK(
    &self,
    (id: I64)
  ) -> BOOKMARK {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_BOOKMARK: stub implementation");
    Default::default()
  }

  fn GET_BOOKMARK_BY_URI(
    &self,
    (uri: STRING)
  ) -> BOOKMARK {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_BOOKMARK_BY_URI: stub implementation");
    Default::default()
  }

  fn LIST_BOOKMARKS(
    &self,
    (limit: I64, offset: I64, filter: BOOKMARK_FILTER)
  ) -> VEC<_B_O_O_K_M_A_R_K> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_BOOKMARKS: stub implementation");
    Vec::new()
  }

  fn UPDATE_BOOKMARK(
    &self,
    (id: I64, title: STRING, notes: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_BOOKMARK: stub implementation");
    false
  }

  fn DELETE_BOOKMARK(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_BOOKMARK: stub implementation");
    false
  }

  fn ADD_CONVERSATION(
    &self,
    (content: VEC<CONVERSATION_ENTRY>, capture_time: DATE, title: STRING, source_url: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_CONVERSATION: stub implementation");
    ()
  }

  fn GET_CONVERSATION(
    &self,
    (id: I64)
  ) -> CONVERSATION {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_CONVERSATION: stub implementation");
    Default::default()
  }

  fn LIST_CONVERSATIONS(
    &self,
    (limit: I64, offset: I64, filter: CONVERSATION_FILTER)
  ) -> VEC<_C_O_N_V_E_R_S_A_T_I_O_N> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_CONVERSATIONS: stub implementation");
    Vec::new()
  }

  fn UPDATE_CONVERSATION(
    &self,
    (id: I64, title: STRING, content: VEC<CONVERSATION_ENTRY>)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_CONVERSATION: stub implementation");
    false
  }

  fn DELETE_CONVERSATION(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_CONVERSATION: stub implementation");
    false
  }

  fn ADD_PARTICIPANT(
    &self,
    (conversation_id: I64, person_id: I64, role: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_PARTICIPANT: stub implementation");
    false
  }

  fn REMOVE_PARTICIPANT(
    &self,
    (conversation_id: I64, person_id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("REMOVE_PARTICIPANT: stub implementation");
    false
  }

  fn LIST_PARTICIPANTS(
    &self,
    (conversation_id: I64)
  ) -> VEC<_I64> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_PARTICIPANTS: stub implementation");
    Vec::new()
  }

  fn ADD_CONVERSATION_MEDIA(
    &self,
    (conversation_id: I64, media_id: I64, context: RECORD<STRING, UNKNOWN>)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_CONVERSATION_MEDIA: stub implementation");
    false
  }

  fn REMOVE_CONVERSATION_MEDIA(
    &self,
    (conversation_id: I64, media_id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("REMOVE_CONVERSATION_MEDIA: stub implementation");
    false
  }

  fn LIST_CONVERSATION_MEDIA(
    &self,
    (conversation_id: I64)
  ) -> VEC<_I64> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_CONVERSATION_MEDIA: stub implementation");
    Vec::new()
  }

  fn GENERATE_PAIRING_CODE(
    &self,
    ()
  ) -> STRING {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GENERATE_PAIRING_CODE: stub implementation");
    String::new()
  }

  fn CONFIRM_PAIRING(
    &self,
    (device_id: STRING, code: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("CONFIRM_PAIRING: stub implementation");
    false
  }

  fn UNPAIR_DEVICE(
    &self,
    (device_id: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UNPAIR_DEVICE: stub implementation");
    ()
  }

  fn LIST_PAIRED_DEVICES(
    &self,
    ()
  ) -> VEC<_S_T_R_I_N_G> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_PAIRED_DEVICES: stub implementation");
    Vec::new()
  }

  fn FOLLOW_DEVICE(
    &self,
    (device_id: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("FOLLOW_DEVICE: stub implementation");
    false
  }

  fn UNFOLLOW_DEVICE(
    &self,
    (device_id: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UNFOLLOW_DEVICE: stub implementation");
    ()
  }

  fn LIST_FOLLOWERS(
    &self,
    ()
  ) -> VEC<_S_T_R_I_N_G> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_FOLLOWERS: stub implementation");
    Vec::new()
  }

  fn IS_FOLLOWING(
    &self,
    (device_id: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("IS_FOLLOWING: stub implementation");
    false
  }

  fn GET_MEDIA(
    &self,
    (id: I64)
  ) -> MEDIA {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_MEDIA: stub implementation");
    Default::default()
  }

  fn GET_MEDIA_BY_HASH(
    &self,
    (content_hash: STRING)
  ) -> MEDIA {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_MEDIA_BY_HASH: stub implementation");
    Default::default()
  }

  fn LIST_MEDIA(
    &self,
    (limit: I64, offset: I64, filter: MEDIA_FILTER)
  ) -> VEC<_M_E_D_I_A> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_MEDIA: stub implementation");
    Vec::new()
  }

  fn UPDATE_MEDIA(
    &self,
    (id: I64, title: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_MEDIA: stub implementation");
    false
  }

  fn DELETE_MEDIA(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_MEDIA: stub implementation");
    false
  }

  fn ADD_MEDIA_LINK(
    &self,
    (uri: STRING, mime_type: STRING, title: STRING, directory: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_MEDIA_LINK: stub implementation");
    ()
  }

  fn ADD_MEDIA_FILE(
    &self,
    (uri: STRING, title: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_MEDIA_FILE: stub implementation");
    ()
  }

  fn REGISTER_SOURCE(
    &self,
    (url: STRING, adapter_type: MEDIA_ADAPTER_TYPE, capabilities: VEC<MEDIA_SOURCE_CAPABILITY>, config: RECORD<STRING, UNKNOWN>)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("REGISTER_SOURCE: stub implementation");
    ()
  }

  fn GET_SOURCE(
    &self,
    (id: I64)
  ) -> MEDIA_SOURCE {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_SOURCE: stub implementation");
    Default::default()
  }

  fn LIST_SOURCES(
    &self,
    (active_only: BOOL, adapter_type: MEDIA_ADAPTER_TYPE)
  ) -> VEC<MEDIA_SOURCE> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_SOURCES: stub implementation");
    Vec::new()
  }

  fn UPDATE_SOURCE(
    &self,
    (id: I64, config: RECORD<STRING, UNKNOWN>, is_active: BOOL)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_SOURCE: stub implementation");
    false
  }

  fn UNREGISTER_SOURCE(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UNREGISTER_SOURCE: stub implementation");
    false
  }

  fn POLL_SOURCE(
    &self,
    (id: I64)
  ) -> POLL_RESULT {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("POLL_SOURCE: stub implementation");
    Default::default()
  }

  fn GET_HEALTH(
    &self,
    (id: I64)
  ) -> MEDIA_SOURCE_HEALTH {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_HEALTH: stub implementation");
    Default::default()
  }

  fn RESET_ERRORS(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("RESET_ERRORS: stub implementation");
    false
  }

  fn GET_NODE_ID(
    &self,
    ()
  ) -> STRING {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_NODE_ID: stub implementation");
    String::new()
  }

  fn IS_NODE_RUNNING(
    &self,
    ()
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("IS_NODE_RUNNING: stub implementation");
    false
  }

  fn GET_NODE_ALIAS(
    &self,
    ()
  ) -> STRING {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_NODE_ALIAS: stub implementation");
    String::new()
  }

  fn START_NODE(
    &self,
    (alias: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("START_NODE: stub implementation");
    false
  }

  fn STOP_NODE(
    &self,
    ()
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("STOP_NODE: stub implementation");
    ()
  }

  fn ADD_PERSON(
    &self,
    (display_name: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_PERSON: stub implementation");
    ()
  }

  fn GET_PERSON(
    &self,
    (id: I64)
  ) -> PERSON {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_PERSON: stub implementation");
    Default::default()
  }

  fn GET_PERSON_BY_HANDLE(
    &self,
    (handle: STRING)
  ) -> PERSON {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_PERSON_BY_HANDLE: stub implementation");
    Default::default()
  }

  fn LIST_PEOPLE(
    &self,
    (limit: I64, offset: I64, filter: PERSON_FILTER)
  ) -> VEC<_P_E_R_S_O_N> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_PEOPLE: stub implementation");
    Vec::new()
  }

  fn UPDATE_PERSON(
    &self,
    (id: I64, display_name: STRING, handle: STRING, metadata: RECORD<STRING, UNKNOWN>)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_PERSON: stub implementation");
    false
  }

  fn DELETE_PERSON(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_PERSON: stub implementation");
    false
  }

  fn CREATE_REPOSITORY(
    &self,
    (name: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("CREATE_REPOSITORY: stub implementation");
    false
  }

  fn LIST_REPOSITORIES(
    &self,
    ()
  ) -> VEC<_S_T_R_I_N_G> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_REPOSITORIES: stub implementation");
    Vec::new()
  }

  fn GET_DEFAULT_REPOSITORY(
    &self,
    ()
  ) -> STRING {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_DEFAULT_REPOSITORY: stub implementation");
    String::new()
  }

  fn SET_DEFAULT_REPOSITORY(
    &self,
    (name: STRING)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("SET_DEFAULT_REPOSITORY: stub implementation");
    false
  }

  fn ADD_POST(
    &self,
    (bits: VEC<ACCUMULABLE_BIT>)
  ) -> I64 {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_POST: stub implementation");
    0
  }

  fn GET_POST(
    &self,
    (id: I64)
  ) -> POST {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_POST: stub implementation");
    Default::default()
  }

  fn LIST_POSTS(
    &self,
    (limit: I64, offset: I64, filter: POST_FILTER)
  ) -> VEC<_P_O_S_T> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_POSTS: stub implementation");
    Vec::new()
  }

  fn UPDATE_POST(
    &self,
    (id: I64, bits: VEC<ACCUMULABLE_BIT>)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("UPDATE_POST: stub implementation");
    false
  }

  fn DELETE_POST(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_POST: stub implementation");
    false
  }

  fn GET_ENTRIES(
    &self,
    (limit: I64, offset: I64, filter: THE_STREAM_ENTRY_FILTER)
  ) -> VEC<THE_STREAM_ENTRY> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_ENTRIES: stub implementation");
    Vec::new()
  }

  fn GET_ENTRY(
    &self,
    (id: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_ENTRY: stub implementation");
    Default::default()
  }

  fn DELETE_ENTRY(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_ENTRY: stub implementation");
    false
  }

  fn GET_ENTRIES_BY_KIND(
    &self,
    (kind: 'MEDIA' | 'POST' | 'BOOKMARK' | 'PERSON' | 'CONVERSATION', limit: I64, before: I64)
  ) -> VEC<THE_STREAM_ENTRY> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_ENTRIES_BY_KIND: stub implementation");
    Vec::new()
  }

  fn SEARCH(
    &self,
    (query: STRING, kinds: VEC<_S_T_R_I_N_G>, limit: I64)
  ) -> VEC<THE_STREAM_ENTRY> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("SEARCH: stub implementation");
    Vec::new()
  }

  fn ADD_BOOKMARK(
    &self,
    (bookmark_id: I64, seen_at: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_BOOKMARK: stub implementation");
    Default::default()
  }

  fn ADD_POST(
    &self,
    (post_id: I64, seen_at: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_POST: stub implementation");
    Default::default()
  }

  fn ADD_MEDIA(
    &self,
    (media_id: I64, seen_at: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_MEDIA: stub implementation");
    Default::default()
  }

  fn ADD_PERSON(
    &self,
    (person_id: I64, seen_at: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_PERSON: stub implementation");
    Default::default()
  }

  fn ADD_CONVERSATION(
    &self,
    (conversation_id: I64, seen_at: I64)
  ) -> THE_STREAM_ENTRY {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_CONVERSATION: stub implementation");
    Default::default()
  }

  fn ADD_VIEW(
    &self,
    (index: I64, badge: STRING, label: STRING)
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("ADD_VIEW: stub implementation");
    ()
  }

  fn GET_VIEW(
    &self,
    (id: I64)
  ) -> VIEW {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("GET_VIEW: stub implementation");
    Default::default()
  }

  fn LIST_VIEWS(
    &self,
    ()
  ) -> VEC<()> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("LIST_VIEWS: stub implementation");
    Vec::new()
  }

  fn DELETE_VIEW(
    &self,
    (id: I64)
  ) -> BOOL {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("DELETE_VIEW: stub implementation");
    false
  }

}
