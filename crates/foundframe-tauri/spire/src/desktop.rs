// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/packages/spire-loom/machinery/bobbin/tauri/desktop.rs.ejs
// Template file: desktop.rs.ejs
// To override: Copy this template to loom/bobbin/tauri/desktop.rs.ejs
// 
// To modify the generated output, edit the template file above.
//! Desktop Platform Implementation - GENERATED by spire-loom
//!
//! On desktop, we initialize o19-foundframe directly and call into it.
//!
//! NOTE: This template generates foundframe-specific access patterns:
//! - thestream operations use with_thestream(|stream| ...)
//! - device_manager operations use with_device_manager(|dm| ...)

use crate::spire::platform::SpireFoundframePlatformTrait;
use crate::spire::error::{Error, Result};
use crate::spire::models::*;
use serde::de::DeserializeOwned;
use std::path::PathBuf;
use tauri::{AppHandle, Manager, Runtime, plugin::PluginApi};

// Import core traits and types needed for method implementations
use o19_foundframe::bookmark::BookmarkStream;
use o19_foundframe::media::MediaStream;
use o19_foundframe::person::PersonStream;
use o19_foundframe::post::PostStream;
use o19_foundframe::conversation::ConversationStream;
use o19_foundframe::device::{DeviceManager, NodeId};
use core::str::FromStr;

/// Desktop platform implementation.
///
/// Holds the foundframe runtime and provides direct access to all operations.
/// 
/// NOTE: This platform does NOT initialize o19-foundframe itself.
/// The Foundframe instance must be created by the caller and passed to 
/// `DesktopPlatform::from_foundframe()`.
pub struct DesktopPlatform<R: Runtime> {
  app_handle: AppHandle<R>,
  foundframe: std::sync::Mutex<Option<o19_foundframe::Foundframe>>,
}

impl<R: Runtime> DesktopPlatform<R> {
  /// Create a DesktopPlatform from an existing Foundframe instance.
  /// 
  /// The caller is responsible for initializing o19-foundframe and passing
  /// the resulting Foundframe instance. This allows the main plugin to control
  /// initialization options, callbacks, and lifecycle.
  pub fn from_foundframe(
    app_handle: AppHandle<R>,
    foundframe: o19_foundframe::Foundframe,
  ) -> Self {
    Self { 
      app_handle,
      foundframe: std::sync::Mutex::new(Some(foundframe)),
    }
  }

  pub fn app_handle(&self) -> &AppHandle<R> {
    &self.app_handle
  }

  /// Get a reference to the underlying Foundframe instance.
  pub fn foundframe(&self) -> std::sync::MutexGuard<Option<o19_foundframe::Foundframe>> {
    self.foundframe.lock().unwrap()
  }
}

fn default_pkb_path() -> PathBuf {
  PathBuf::from("pkb")
}

impl<R: Runtime> SpireFoundframePlatformTrait for DesktopPlatform<R> {
  fn exit(&self, code: i32) {
    self.app_handle.exit(code);
  }

  fn request_permissions(&self) -> Result<NotificationPermissionStatus> {
    Ok(NotificationPermissionStatus {
      status: "granted".into(),
    })
  }

  fn shutdown(&self) -> Result<()> {
    // TODO: Shutdown o19-foundframe
    Ok(())
  }

  fn bookmark_add_bookmark(
    &self,
        url: String,
        title: Option<String>,
        notes: Option<String>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_add_bookmark: stub implementation");
    ()
  }

  fn bookmark_get_bookmark(
    &self,
        id: i64
    
  ) -> Bookmark {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_get_bookmark: stub implementation");
    // Entity type: Bookmark
    Default::default()
  }

  fn bookmark_get_bookmark_by_uri(
    &self,
        uri: String
    
  ) -> Bookmark {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_get_bookmark_by_uri: stub implementation");
    // Entity type: Bookmark
    Default::default()
  }

  fn bookmark_list_bookmarks(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<BookmarkFilter>
    
  ) -> Vec<Bookmark[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_list_bookmarks: stub implementation");
    Vec::new()
  }

  fn bookmark_update_bookmark(
    &self,
        id: i64,
        title: Option<String>,
        notes: Option<String>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_update_bookmark: stub implementation");
    false
  }

  fn bookmark_delete_bookmark(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("bookmark_delete_bookmark: stub implementation");
    false
  }

  fn conversation_add_conversation(
    &self,
        content: ConversationEntry[],
        capture_time: Date,
        title: Option<String>,
        source_url: Option<String>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_add_conversation: stub implementation");
    ()
  }

  fn conversation_get_conversation(
    &self,
        id: i64
    
  ) -> Conversation {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_get_conversation: stub implementation");
    // Entity type: Conversation
    Default::default()
  }

  fn conversation_list_conversations(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<ConversationFilter>
    
  ) -> Vec<Conversation[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_list_conversations: stub implementation");
    Vec::new()
  }

  fn conversation_update_conversation(
    &self,
        id: i64,
        title: Option<String>,
        content: Option<ConversationEntry[]>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_update_conversation: stub implementation");
    false
  }

  fn conversation_delete_conversation(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_delete_conversation: stub implementation");
    false
  }

  fn conversation_add_participant(
    &self,
        conversation_id: i64,
        person_id: i64,
        role: Option<String>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_add_participant: stub implementation");
    false
  }

  fn conversation_remove_participant(
    &self,
        conversation_id: i64,
        person_id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_remove_participant: stub implementation");
    false
  }

  fn conversation_list_participants(
    &self,
        conversation_id: i64
    
  ) -> Vec<number[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_list_participants: stub implementation");
    Vec::new()
  }

  fn conversation_add_conversation_media(
    &self,
        conversation_id: i64,
        media_id: i64,
        context: Option<Record<string, unknown>>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_add_conversation_media: stub implementation");
    false
  }

  fn conversation_remove_conversation_media(
    &self,
        conversation_id: i64,
        media_id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_remove_conversation_media: stub implementation");
    false
  }

  fn conversation_list_conversation_media(
    &self,
        conversation_id: i64
    
  ) -> Vec<number[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("conversation_list_conversation_media: stub implementation");
    Vec::new()
  }

  fn device_generate_pairing_code(
    &self,
    
  ) -> String {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_generate_pairing_code: stub implementation");
    String::new()
  }

  fn device_confirm_pairing(
    &self,
        device_id: String,
        code: String
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_confirm_pairing: stub implementation");
    false
  }

  fn device_unpair_device(
    &self,
        device_id: String
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_unpair_device: stub implementation");
    ()
  }

  fn device_list_paired_devices(
    &self,
    
  ) -> Vec<string[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_list_paired_devices: stub implementation");
    Vec::new()
  }

  fn device_follow_device(
    &self,
        device_id: String
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_follow_device: stub implementation");
    false
  }

  fn device_unfollow_device(
    &self,
        device_id: String
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_unfollow_device: stub implementation");
    ()
  }

  fn device_list_followers(
    &self,
    
  ) -> Vec<string[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_list_followers: stub implementation");
    Vec::new()
  }

  fn device_is_following(
    &self,
        device_id: String
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("device_is_following: stub implementation");
    false
  }

  fn media_get_media(
    &self,
        id: i64
    
  ) -> Media {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_get_media: stub implementation");
    // Entity type: Media
    Default::default()
  }

  fn media_get_media_by_hash(
    &self,
        content_hash: String
    
  ) -> Media {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_get_media_by_hash: stub implementation");
    // Entity type: Media
    Default::default()
  }

  fn media_list_media(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<MediaFilter>
    
  ) -> Vec<Media[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_list_media: stub implementation");
    Vec::new()
  }

  fn media_update_media(
    &self,
        id: i64,
        title: Option<String>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_update_media: stub implementation");
    false
  }

  fn media_delete_media(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_delete_media: stub implementation");
    false
  }

  fn media_add_media_link(
    &self,
        uri: String,
        mime_type: String,
        title: Option<String>,
        directory: Option<String>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_add_media_link: stub implementation");
    ()
  }

  fn media_add_media_file(
    &self,
        uri: String,
        title: Option<String>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_add_media_file: stub implementation");
    ()
  }

  fn media_source_register_source(
    &self,
        url: String,
        adapter_type: MediaAdapterType,
        capabilities: MediaSourceCapability[],
        config: Option<Record<string, unknown>>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_register_source: stub implementation");
    ()
  }

  fn media_source_get_source(
    &self,
        id: i64
    
  ) -> MediaSource {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_get_source: stub implementation");
    // Entity type: MediaSource
    Default::default()
  }

  fn media_source_list_sources(
    &self,
        active_only: Option<bool>,
        adapter_type: Option<MediaAdapterType>
    
  ) -> Vec<MediaSource[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_list_sources: stub implementation");
    Vec::new()
  }

  fn media_source_update_source(
    &self,
        id: i64,
        config: Option<Record<string, unknown>>,
        is_active: Option<bool>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_update_source: stub implementation");
    false
  }

  fn media_source_unregister_source(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_unregister_source: stub implementation");
    false
  }

  fn media_source_poll_source(
    &self,
        id: i64
    
  ) -> PollResult {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_poll_source: stub implementation");
    // Entity type: PollResult
    Default::default()
  }

  fn media_source_get_health(
    &self,
        id: i64
    
  ) -> MediaSourceHealth {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_get_health: stub implementation");
    // Entity type: MediaSourceHealth
    Default::default()
  }

  fn media_source_reset_errors(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("media_source_reset_errors: stub implementation");
    false
  }

  fn person_add_person(
    &self,
        display_name: String,
        handle: Option<String>,
        metadata: Option<Record<string, unknown>>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_add_person: stub implementation");
    ()
  }

  fn person_get_person(
    &self,
        id: i64
    
  ) -> Person {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_get_person: stub implementation");
    // Entity type: Person
    Default::default()
  }

  fn person_get_person_by_handle(
    &self,
        handle: String
    
  ) -> Person {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_get_person_by_handle: stub implementation");
    // Entity type: Person
    Default::default()
  }

  fn person_list_people(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<PersonFilter>
    
  ) -> Vec<Person[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_list_people: stub implementation");
    Vec::new()
  }

  fn person_update_person(
    &self,
        id: i64,
        display_name: Option<String>,
        handle: Option<String>,
        metadata: Option<Record<string, unknown>>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_update_person: stub implementation");
    false
  }

  fn person_delete_person(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("person_delete_person: stub implementation");
    false
  }

  fn post_add_post(
    &self,
        bits: AccumulableBit[]
    
  ) -> i64 {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("post_add_post: stub implementation");
    0
  }

  fn post_get_post(
    &self,
        id: i64
    
  ) -> Post {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("post_get_post: stub implementation");
    // Entity type: Post
    Default::default()
  }

  fn post_list_posts(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<PostFilter>
    
  ) -> Vec<Post[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("post_list_posts: stub implementation");
    Vec::new()
  }

  fn post_update_post(
    &self,
        id: i64,
        bits: Option<AccumulableBit[]>
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("post_update_post: stub implementation");
    false
  }

  fn post_delete_post(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("post_delete_post: stub implementation");
    false
  }

  fn the_stream_get_entries(
    &self,
        limit: Option<i64>,
        offset: Option<i64>,
        filter: Option<TheStreamEntryFilter>
    
  ) -> Vec<TheStreamEntry[]> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_get_entries: stub implementation");
    Vec::new()
  }

  fn the_stream_get_entry(
    &self,
        id: i64
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_get_entry: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn the_stream_delete_entry(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_delete_entry: stub implementation");
    false
  }

  fn the_stream_get_entries_by_kind(
    &self,
        kind: 'media' | 'post' | 'bookmark' | 'person' | 'conversation',
        limit: Option<i64>,
        before: Option<i64>
    
  ) -> TheStreamEntry[] {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_get_entries_by_kind: stub implementation");
    // Entity type: TheStreamEntry[]
    Default::default()
  }

  fn the_stream_search(
    &self,
        query: String,
        kinds: Option<string[]>,
        limit: Option<i64>
    
  ) -> TheStreamEntry[] {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_search: stub implementation");
    // Entity type: TheStreamEntry[]
    Default::default()
  }

  fn the_stream_add_bookmark(
    &self,
        bookmark_id: i64,
        seen_at: Option<i64>
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_add_bookmark: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn the_stream_add_post(
    &self,
        post_id: i64,
        seen_at: Option<i64>
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_add_post: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn the_stream_add_media(
    &self,
        media_id: i64,
        seen_at: Option<i64>
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_add_media: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn the_stream_add_person(
    &self,
        person_id: i64,
        seen_at: Option<i64>
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_add_person: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn the_stream_add_conversation(
    &self,
        conversation_id: i64,
        seen_at: Option<i64>
    
  ) -> TheStreamEntry {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("the_stream_add_conversation: stub implementation");
    // Entity type: TheStreamEntry
    Default::default()
  }

  fn view_add_view(
    &self,
        index: i64,
        badge: String,
        label: Option<String>
    
  ) -> () {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("view_add_view: stub implementation");
    ()
  }

  fn view_get_view(
    &self,
        id: i64
    
  ) -> View {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("view_get_view: stub implementation");
    // Entity type: View
    Default::default()
  }

  fn view_list_views(
    &self,
    
  ) -> Vec<()> {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("view_list_views: stub implementation");
    Vec::new()
  }

  fn view_delete_view(
    &self,
        id: i64
    
  ) -> bool {
    // Direct access (no field link)
    // STUB: Non-Result return from direct access
    tracing::warn!("view_delete_view: stub implementation");
    false
  }

}
