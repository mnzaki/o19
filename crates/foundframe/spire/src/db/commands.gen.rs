// GENERATED BY SPIRE-LOOM - DO NOT EDIT (even if LLM)
// 
// This file is automatically generated from a template.
// Changes will be overwritten on next generation.
// 
// Template: /home/mnzaki/Projects/circulari.ty/o19/crates/foundframe/loom/bobbin/rust/db/db_command.rs.ejs
// Template file: db_command.rs.ejs
// This is a workspace custom template (from loom/bobbin/)
// 
// To modify the generated output, edit the template file above.
// GENERATED BY db-binding Treadle - Do not edit

use crate::Result;
use crate::db::oneshot;
use sea_query::{Cond, Condition, Expr, Iden, SimpleExpr};
use crate::db::bookmark_data::BookmarkData;
use crate::db::conversation_data::ConversationData;
use crate::db::media_data::MediaData;
use crate::db::mediasource_data::MediaSourceData;
use crate::db::person_data::PersonData;
use crate::db::post_data::PostData;
use crate::db::streamentry_data::StreamEntryData;
use crate::db::view_data::ViewData;

/// Filter criteria for Bookmark list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct BookmarkFilter {
  /// Filter by uri (exact match)
  pub uri: Option<string>,
  /// Filter by title (exact match)
  pub title: Option<Option<string>>,
  /// Filter by notes (exact match)
  pub notes: Option<Option<string>>,
  /// Filter by creationContext (exact match)
  pub creationContext: Option<serde_json::Value>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl BookmarkFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.uri.is_none()
      && self.title.is_none()
      && self.notes.is_none()
      && self.creationContext.is_none()
      && self.createdAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(v) = self.uri {
      exprs.push(Expr::col(BookmarkIden::Uri).eq(v).into());
    }
    if let Some(ref v) = self.title {
      exprs.push(Expr::col(BookmarkIden::Title).eq(v.clone()).into());
    }
    if let Some(ref v) = self.notes {
      exprs.push(Expr::col(BookmarkIden::Notes).eq(v.clone()).into());
    }
    if let Some(ref v) = self.creationContext {
      exprs.push(Expr::col(BookmarkIden::CreationContext).eq(v.to_string()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(BookmarkIden::CreatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(BookmarkIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(BookmarkIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for Bookmark table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum BookmarkIden {
  Table,
  Id,
  Uri,
  Title,
  Notes,
  CreationContext,
  CreatedAt,
}

impl Iden for BookmarkIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "bookmark",
        Self::Id => "id",
        Self::Uri => "uri",
        Self::Title => "title",
        Self::Notes => "notes",
        Self::CreationContext => "creation_context",
        Self::CreatedAt => "created_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for Conversation list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct ConversationFilter {
  /// Filter by title (exact match)
  pub title: Option<Option<string>>,
  /// Filter by content (exact match)
  pub content: Option<serde_json::Value>,
  /// Filter by attributedTo (exact match)
  pub attributedTo: Option<Option<string>>,
  /// Filter by captureTime (exact match)
  pub captureTime: Option<number>,
  /// Filter by firstEntryTime (exact match)
  pub firstEntryTime: Option<Option<number>>,
  /// Filter by lastEntryTime (exact match)
  pub lastEntryTime: Option<Option<number>>,
  /// Filter by sourceUrl (exact match)
  pub sourceUrl: Option<Option<string>>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Filter by updatedAt (exact match)
  pub updatedAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl ConversationFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.title.is_none()
      && self.content.is_none()
      && self.attributedTo.is_none()
      && self.captureTime.is_none()
      && self.firstEntryTime.is_none()
      && self.lastEntryTime.is_none()
      && self.sourceUrl.is_none()
      && self.createdAt.is_none()
      && self.updatedAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(ref v) = self.title {
      exprs.push(Expr::col(ConversationIden::Title).eq(v.clone()).into());
    }
    if let Some(ref v) = self.content {
      exprs.push(Expr::col(ConversationIden::Content).eq(v.to_string()).into());
    }
    if let Some(ref v) = self.attributedTo {
      exprs.push(Expr::col(ConversationIden::AttributedTo).eq(v.clone()).into());
    }
    if let Some(v) = self.captureTime {
      exprs.push(Expr::col(ConversationIden::CaptureTime).eq(v).into());
    }
    if let Some(ref v) = self.firstEntryTime {
      exprs.push(Expr::col(ConversationIden::FirstEntryTime).eq(v.clone()).into());
    }
    if let Some(ref v) = self.lastEntryTime {
      exprs.push(Expr::col(ConversationIden::LastEntryTime).eq(v.clone()).into());
    }
    if let Some(ref v) = self.sourceUrl {
      exprs.push(Expr::col(ConversationIden::SourceUrl).eq(v.clone()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(ConversationIden::CreatedAt).eq(v).into());
    }
    if let Some(v) = self.updatedAt {
      exprs.push(Expr::col(ConversationIden::UpdatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(ConversationIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(ConversationIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for Conversation table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ConversationIden {
  Table,
  Id,
  Title,
  Content,
  AttributedTo,
  CaptureTime,
  FirstEntryTime,
  LastEntryTime,
  SourceUrl,
  CreatedAt,
  UpdatedAt,
}

impl Iden for ConversationIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "conversation",
        Self::Id => "id",
        Self::Title => "title",
        Self::Content => "content",
        Self::AttributedTo => "attributed_to",
        Self::CaptureTime => "capture_time",
        Self::FirstEntryTime => "first_entry_time",
        Self::LastEntryTime => "last_entry_time",
        Self::SourceUrl => "source_url",
        Self::CreatedAt => "created_at",
        Self::UpdatedAt => "updated_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for Media list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct MediaFilter {
  /// Filter by contentHash (exact match)
  pub contentHash: Option<Option<string>>,
  /// Filter by mimeType (exact match)
  pub mimeType: Option<string>,
  /// Filter by uri (exact match)
  pub uri: Option<string>,
  /// Filter by width (exact match)
  pub width: Option<Option<number>>,
  /// Filter by height (exact match)
  pub height: Option<Option<number>>,
  /// Filter by durationMs (exact match)
  pub durationMs: Option<Option<number>>,
  /// Filter by metadata (exact match)
  pub metadata: Option<Option<serde_json::Value>>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl MediaFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.contentHash.is_none()
      && self.mimeType.is_none()
      && self.uri.is_none()
      && self.width.is_none()
      && self.height.is_none()
      && self.durationMs.is_none()
      && self.metadata.as_ref().map_or(true, |v| v.is_none())
      && self.createdAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(ref v) = self.contentHash {
      exprs.push(Expr::col(MediaIden::ContentHash).eq(v.clone()).into());
    }
    if let Some(v) = self.mimeType {
      exprs.push(Expr::col(MediaIden::MimeType).eq(v).into());
    }
    if let Some(v) = self.uri {
      exprs.push(Expr::col(MediaIden::Uri).eq(v).into());
    }
    if let Some(ref v) = self.width {
      exprs.push(Expr::col(MediaIden::Width).eq(v.clone()).into());
    }
    if let Some(ref v) = self.height {
      exprs.push(Expr::col(MediaIden::Height).eq(v.clone()).into());
    }
    if let Some(ref v) = self.durationMs {
      exprs.push(Expr::col(MediaIden::DurationMs).eq(v.clone()).into());
    }
    if let Some(Some(ref v)) = self.metadata {
      exprs.push(Expr::col(MediaIden::Metadata).eq(v.to_string()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(MediaIden::CreatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(MediaIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(MediaIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for Media table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MediaIden {
  Table,
  Id,
  ContentHash,
  MimeType,
  Uri,
  Width,
  Height,
  DurationMs,
  Metadata,
  CreatedAt,
}

impl Iden for MediaIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "media",
        Self::Id => "id",
        Self::ContentHash => "content_hash",
        Self::MimeType => "mime_type",
        Self::Uri => "uri",
        Self::Width => "width",
        Self::Height => "height",
        Self::DurationMs => "duration_ms",
        Self::Metadata => "metadata",
        Self::CreatedAt => "created_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for MediaSource list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct MediaSourceFilter {
  /// Filter by url (exact match)
  pub url: Option<string>,
  /// Filter by adapterType (exact match)
  pub adapterType: Option<string>,
  /// Filter by cursorState (exact match)
  pub cursorState: Option<Option<serde_json::Value>>,
  /// Filter by capabilities (exact match)
  pub capabilities: Option<serde_json::Value>,
  /// Filter by config (exact match)
  pub config: Option<Option<serde_json::Value>>,
  /// Filter by lastPolledAt (exact match)
  pub lastPolledAt: Option<Option<number>>,
  /// Filter by lastError (exact match)
  pub lastError: Option<Option<string>>,
  /// Filter by isActive (exact match)
  pub isActive: Option<boolean>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Filter by updatedAt (exact match)
  pub updatedAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl MediaSourceFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.url.is_none()
      && self.adapterType.is_none()
      && self.cursorState.as_ref().map_or(true, |v| v.is_none())
      && self.capabilities.is_none()
      && self.config.as_ref().map_or(true, |v| v.is_none())
      && self.lastPolledAt.is_none()
      && self.lastError.is_none()
      && self.isActive.is_none()
      && self.createdAt.is_none()
      && self.updatedAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(v) = self.url {
      exprs.push(Expr::col(MediaSourceIden::Url).eq(v).into());
    }
    if let Some(v) = self.adapterType {
      exprs.push(Expr::col(MediaSourceIden::AdapterType).eq(v).into());
    }
    if let Some(Some(ref v)) = self.cursorState {
      exprs.push(Expr::col(MediaSourceIden::CursorState).eq(v.to_string()).into());
    }
    if let Some(ref v) = self.capabilities {
      exprs.push(Expr::col(MediaSourceIden::Capabilities).eq(v.to_string()).into());
    }
    if let Some(Some(ref v)) = self.config {
      exprs.push(Expr::col(MediaSourceIden::Config).eq(v.to_string()).into());
    }
    if let Some(ref v) = self.lastPolledAt {
      exprs.push(Expr::col(MediaSourceIden::LastPolledAt).eq(v.clone()).into());
    }
    if let Some(ref v) = self.lastError {
      exprs.push(Expr::col(MediaSourceIden::LastError).eq(v.clone()).into());
    }
    if let Some(v) = self.isActive {
      exprs.push(Expr::col(MediaSourceIden::IsActive).eq(v).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(MediaSourceIden::CreatedAt).eq(v).into());
    }
    if let Some(v) = self.updatedAt {
      exprs.push(Expr::col(MediaSourceIden::UpdatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(MediaSourceIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(MediaSourceIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for MediaSource table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MediaSourceIden {
  Table,
  Id,
  Url,
  AdapterType,
  CursorState,
  Capabilities,
  Config,
  LastPolledAt,
  LastError,
  IsActive,
  CreatedAt,
  UpdatedAt,
}

impl Iden for MediaSourceIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "media_source",
        Self::Id => "id",
        Self::Url => "url",
        Self::AdapterType => "adapter_type",
        Self::CursorState => "cursor_state",
        Self::Capabilities => "capabilities",
        Self::Config => "config",
        Self::LastPolledAt => "last_polled_at",
        Self::LastError => "last_error",
        Self::IsActive => "is_active",
        Self::CreatedAt => "created_at",
        Self::UpdatedAt => "updated_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for Person list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct PersonFilter {
  /// Filter by displayName (exact match)
  pub displayName: Option<string>,
  /// Filter by handle (exact match)
  pub handle: Option<Option<string>>,
  /// Filter by avatarMediaId (exact match)
  pub avatarMediaId: Option<Option<number>>,
  /// Filter by metadata (exact match)
  pub metadata: Option<Option<serde_json::Value>>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Filter by updatedAt (exact match)
  pub updatedAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl PersonFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.displayName.is_none()
      && self.handle.is_none()
      && self.avatarMediaId.is_none()
      && self.metadata.as_ref().map_or(true, |v| v.is_none())
      && self.createdAt.is_none()
      && self.updatedAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(v) = self.displayName {
      exprs.push(Expr::col(PersonIden::DisplayName).eq(v).into());
    }
    if let Some(ref v) = self.handle {
      exprs.push(Expr::col(PersonIden::Handle).eq(v.clone()).into());
    }
    if let Some(ref v) = self.avatarMediaId {
      exprs.push(Expr::col(PersonIden::AvatarMediaId).eq(v.clone()).into());
    }
    if let Some(Some(ref v)) = self.metadata {
      exprs.push(Expr::col(PersonIden::Metadata).eq(v.to_string()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(PersonIden::CreatedAt).eq(v).into());
    }
    if let Some(v) = self.updatedAt {
      exprs.push(Expr::col(PersonIden::UpdatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(PersonIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(PersonIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for Person table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PersonIden {
  Table,
  Id,
  DisplayName,
  Handle,
  AvatarMediaId,
  Metadata,
  CreatedAt,
  UpdatedAt,
}

impl Iden for PersonIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "person",
        Self::Id => "id",
        Self::DisplayName => "display_name",
        Self::Handle => "handle",
        Self::AvatarMediaId => "avatar_media_id",
        Self::Metadata => "metadata",
        Self::CreatedAt => "created_at",
        Self::UpdatedAt => "updated_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for Post list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct PostFilter {
  /// Filter by bits (exact match)
  pub bits: Option<serde_json::Value>,
  /// Filter by contentHash (exact match)
  pub contentHash: Option<Option<string>>,
  /// Filter by authorDid (exact match)
  pub authorDid: Option<Option<string>>,
  /// Filter by signature (exact match)
  pub signature: Option<Option<string>>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Filter by updatedAt (exact match)
  pub updatedAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl PostFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.bits.is_none()
      && self.contentHash.is_none()
      && self.authorDid.is_none()
      && self.signature.is_none()
      && self.createdAt.is_none()
      && self.updatedAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(ref v) = self.bits {
      exprs.push(Expr::col(PostIden::Bits).eq(v.to_string()).into());
    }
    if let Some(ref v) = self.contentHash {
      exprs.push(Expr::col(PostIden::ContentHash).eq(v.clone()).into());
    }
    if let Some(ref v) = self.authorDid {
      exprs.push(Expr::col(PostIden::AuthorDid).eq(v.clone()).into());
    }
    if let Some(ref v) = self.signature {
      exprs.push(Expr::col(PostIden::Signature).eq(v.clone()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(PostIden::CreatedAt).eq(v).into());
    }
    if let Some(v) = self.updatedAt {
      exprs.push(Expr::col(PostIden::UpdatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(PostIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(PostIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for Post table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PostIden {
  Table,
  Id,
  Bits,
  ContentHash,
  AuthorDid,
  Signature,
  CreatedAt,
  UpdatedAt,
}

impl Iden for PostIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "post",
        Self::Id => "id",
        Self::Bits => "bits",
        Self::ContentHash => "content_hash",
        Self::AuthorDid => "author_did",
        Self::Signature => "signature",
        Self::CreatedAt => "created_at",
        Self::UpdatedAt => "updated_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for StreamEntry list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct StreamEntryFilter {
  /// Filter by seenAt (exact match)
  pub seenAt: Option<number>,
  /// Filter by personId (exact match)
  pub personId: Option<Option<number>>,
  /// Filter by postId (exact match)
  pub postId: Option<Option<number>>,
  /// Filter by mediaId (exact match)
  pub mediaId: Option<Option<number>>,
  /// Filter by bookmarkId (exact match)
  pub bookmarkId: Option<Option<number>>,
  /// Filter by conversationId (exact match)
  pub conversationId: Option<Option<number>>,
  /// Filter by directory (exact match)
  pub directory: Option<Option<string>>,
  /// Filter by kind (exact match)
  pub kind: Option<Option<string>>,
  /// Filter by contentHash (exact match)
  pub contentHash: Option<Option<string>>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Only return entries with seenAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with seenAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl StreamEntryFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.seenAt.is_none()
      && self.personId.is_none()
      && self.postId.is_none()
      && self.mediaId.is_none()
      && self.bookmarkId.is_none()
      && self.conversationId.is_none()
      && self.directory.is_none()
      && self.kind.is_none()
      && self.contentHash.is_none()
      && self.createdAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(v) = self.seenAt {
      exprs.push(Expr::col(StreamEntryIden::SeenAt).eq(v).into());
    }
    if let Some(ref v) = self.personId {
      exprs.push(Expr::col(StreamEntryIden::PersonId).eq(v.clone()).into());
    }
    if let Some(ref v) = self.postId {
      exprs.push(Expr::col(StreamEntryIden::PostId).eq(v.clone()).into());
    }
    if let Some(ref v) = self.mediaId {
      exprs.push(Expr::col(StreamEntryIden::MediaId).eq(v.clone()).into());
    }
    if let Some(ref v) = self.bookmarkId {
      exprs.push(Expr::col(StreamEntryIden::BookmarkId).eq(v.clone()).into());
    }
    if let Some(ref v) = self.conversationId {
      exprs.push(Expr::col(StreamEntryIden::ConversationId).eq(v.clone()).into());
    }
    if let Some(ref v) = self.directory {
      exprs.push(Expr::col(StreamEntryIden::Directory).eq(v.clone()).into());
    }
    if let Some(ref v) = self.kind {
      exprs.push(Expr::col(StreamEntryIden::Kind).eq(v.clone()).into());
    }
    if let Some(ref v) = self.contentHash {
      exprs.push(Expr::col(StreamEntryIden::ContentHash).eq(v.clone()).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(StreamEntryIden::CreatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(StreamEntryIden::SeenAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(StreamEntryIden::SeenAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for StreamEntry table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum StreamEntryIden {
  Table,
  Id,
  SeenAt,
  PersonId,
  PostId,
  MediaId,
  BookmarkId,
  ConversationId,
  Directory,
  Kind,
  ContentHash,
  CreatedAt,
}

impl Iden for StreamEntryIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "stream_entry",
        Self::Id => "id",
        Self::SeenAt => "seen_at",
        Self::PersonId => "person_id",
        Self::PostId => "post_id",
        Self::MediaId => "media_id",
        Self::BookmarkId => "bookmark_id",
        Self::ConversationId => "conversation_id",
        Self::Directory => "directory",
        Self::Kind => "kind",
        Self::ContentHash => "content_hash",
        Self::CreatedAt => "created_at",
      }
    ).unwrap();
  }
}

/// Filter criteria for View list queries.
///
/// Only specified filters are applied (fields are Option).
#[derive(Debug, Clone, Default)]
pub struct ViewFilter {
  /// Filter by index (exact match)
  pub index: Option<number>,
  /// Filter by label (exact match)
  pub label: Option<Option<string>>,
  /// Filter by badge (exact match)
  pub badge: Option<string>,
  /// Filter by filters (exact match)
  pub filters: Option<serde_json::Value>,
  /// Filter by sortBy (exact match)
  pub sortBy: Option<string>,
  /// Filter by isPinned (exact match)
  pub isPinned: Option<boolean>,
  /// Filter by isThestream (exact match)
  pub isThestream: Option<boolean>,
  /// Filter by createdAt (exact match)
  pub createdAt: Option<i64>,
  /// Filter by updatedAt (exact match)
  pub updatedAt: Option<i64>,
  /// Only return entries with createdAt >= this value (inclusive)
  pub after: Option<i64>,
  /// Only return entries with createdAt <= this value (inclusive)
  pub before: Option<i64>,
}

impl ViewFilter {
  /// Create a new empty filter
  pub fn new() -> Self {
    Self::default()
  }
  
  /// Check if any filters are set
  pub fn is_empty(&self) -> bool {
    self.after.is_none() && self.before.is_none()
      && self.index.is_none()
      && self.label.is_none()
      && self.badge.is_none()
      && self.filters.is_none()
      && self.sortBy.is_none()
      && self.isPinned.is_none()
      && self.isThestream.is_none()
      && self.createdAt.is_none()
      && self.updatedAt.is_none()
  }
  
  /// Collect all active filter expressions.
  fn collect_exprs(&self) -> Vec<SimpleExpr> {
    let mut exprs = Vec::new();
    
    if let Some(v) = self.index {
      exprs.push(Expr::col(ViewIden::Index).eq(v).into());
    }
    if let Some(ref v) = self.label {
      exprs.push(Expr::col(ViewIden::Label).eq(v.clone()).into());
    }
    if let Some(v) = self.badge {
      exprs.push(Expr::col(ViewIden::Badge).eq(v).into());
    }
    if let Some(ref v) = self.filters {
      exprs.push(Expr::col(ViewIden::Filters).eq(v.to_string()).into());
    }
    if let Some(v) = self.sortBy {
      exprs.push(Expr::col(ViewIden::SortBy).eq(v).into());
    }
    if let Some(v) = self.isPinned {
      exprs.push(Expr::col(ViewIden::IsPinned).eq(v).into());
    }
    if let Some(v) = self.isThestream {
      exprs.push(Expr::col(ViewIden::IsThestream).eq(v).into());
    }
    if let Some(v) = self.createdAt {
      exprs.push(Expr::col(ViewIden::CreatedAt).eq(v).into());
    }
    if let Some(v) = self.updatedAt {
      exprs.push(Expr::col(ViewIden::UpdatedAt).eq(v).into());
    }
    
    // Temporal bounds (using seen_at or created_at)
    if let Some(v) = self.after {
      exprs.push(Expr::col(ViewIden::CreatedAt).gte(v).into());
    }
    if let Some(v) = self.before {
      exprs.push(Expr::col(ViewIden::CreatedAt).lte(v).into());
    }
    
    exprs
  }
  
  /// Convert this filter to a sea-query Condition.
  pub fn to_condition(&self) -> Condition {
    let exprs = self.collect_exprs();
    if exprs.is_empty() {
      return Cond::all();
    }
    
    let mut cond = Cond::all();
    for expr in exprs {
      cond = cond.add(expr);
    }
    cond
  }
}

/// Iden for View table columns.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ViewIden {
  Table,
  Id,
  Index,
  Label,
  Badge,
  Filters,
  SortBy,
  IsPinned,
  IsThestream,
  CreatedAt,
  UpdatedAt,
}

impl Iden for ViewIden {
  fn unquoted(&self, s: &mut dyn std::fmt::Write) {
    write!(
      s,
      "{}",
      match self {
        Self::Table => "view",
        Self::Id => "id",
        Self::Index => "index",
        Self::Label => "label",
        Self::Badge => "badge",
        Self::Filters => "filters",
        Self::SortBy => "sort_by",
        Self::IsPinned => "is_pinned",
        Self::IsThestream => "is_thestream",
        Self::CreatedAt => "created_at",
        Self::UpdatedAt => "updated_at",
      }
    ).unwrap();
  }
}


/// Unified filter type alias for TheStreamEntry (primary use case).
//pub type ListFilter = BookmarkFilter;

/// Commands sent to the database actor.
///
/// This enum contains all CRUD operations for all entities.
/// Generated from loom entity definitions.
#[derive(Debug)]
pub enum DbCommand {
  // ============================================================================
  // Bookmark Commands
  // ============================================================================
  /// Create new bookmark
  CreateBookmark {
    data: BookmarkData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get bookmark by ID
  ReadBookmarkById {
    id: i64,
    respond: oneshot::Sender<Result<Option<BookmarkData>>>,
  },
  /// Get bookmark by URL (convenience lookup)
  ReadBookmarkByUrl {
    url: String,
    respond: oneshot::Sender<Result<Option<BookmarkData>>>,
  },
  
  /// List bookmarks with optional filtering
  ListBookmarks {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: BookmarkFilter,
    respond: oneshot::Sender<Result<Vec<BookmarkData>>>,
  },
  
  /// Update bookmark
  UpdateBookmark {
    id: i64,
    data: BookmarkData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update bookmark's title field
  UpdateBookmarkTitle {
    id: i64,
    title: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update bookmark's notes field
  UpdateBookmarkNotes {
    id: i64,
    notes: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete bookmark
  DeleteBookmark {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // Conversation Commands
  // ============================================================================
  /// Create new conversation
  CreateConversation {
    data: ConversationData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get conversation by ID
  ReadConversationById {
    id: i64,
    respond: oneshot::Sender<Result<Option<ConversationData>>>,
  },
  
  /// List conversations with optional filtering
  ListConversations {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: ConversationFilter,
    respond: oneshot::Sender<Result<Vec<ConversationData>>>,
  },
  
  /// Update conversation
  UpdateConversation {
    id: i64,
    data: ConversationData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's title field
  UpdateConversationTitle {
    id: i64,
    title: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's attributedTo field
  UpdateConversationAttributedTo {
    id: i64,
    attributedTo: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's firstEntryTime field
  UpdateConversationFirstEntryTime {
    id: i64,
    firstEntryTime: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's lastEntryTime field
  UpdateConversationLastEntryTime {
    id: i64,
    lastEntryTime: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's sourceUrl field
  UpdateConversationSourceUrl {
    id: i64,
    sourceUrl: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update conversation's updatedAt field
  UpdateConversationUpdatedAt {
    id: i64,
    updatedAt: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete conversation
  DeleteConversation {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // Media Commands
  // ============================================================================
  /// Create new media
  CreateMedia {
    data: MediaData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get media by ID
  ReadMediaById {
    id: i64,
    respond: oneshot::Sender<Result<Option<MediaData>>>,
  },
  /// Get media by URL (convenience lookup)
  ReadMediaByUrl {
    url: String,
    respond: oneshot::Sender<Result<Option<MediaData>>>,
  },
  
  /// List medias with optional filtering
  ListMedias {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: MediaFilter,
    respond: oneshot::Sender<Result<Vec<MediaData>>>,
  },
  
  /// Update media
  UpdateMedia {
    id: i64,
    data: MediaData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update media's contentHash field
  UpdateMediaContentHash {
    id: i64,
    contentHash: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update media's width field
  UpdateMediaWidth {
    id: i64,
    width: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update media's height field
  UpdateMediaHeight {
    id: i64,
    height: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update media's durationMs field
  UpdateMediaDurationMs {
    id: i64,
    durationMs: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update media's metadata field
  UpdateMediaMetadata {
    id: i64,
    metadata: Option<serde_json::Value>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete media
  DeleteMedia {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // MediaSource Commands
  // ============================================================================
  /// Create new mediasource
  CreateMediaSource {
    data: MediaSourceData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get mediasource by ID
  ReadMediaSourceById {
    id: i64,
    respond: oneshot::Sender<Result<Option<MediaSourceData>>>,
  },
  /// Get mediasource by URL (convenience lookup)
  ReadMediaSourceByUrl {
    url: String,
    respond: oneshot::Sender<Result<Option<MediaSourceData>>>,
  },
  
  /// List mediasources with optional filtering
  ListMediaSources {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: MediaSourceFilter,
    respond: oneshot::Sender<Result<Vec<MediaSourceData>>>,
  },
  
  /// Update mediasource
  UpdateMediaSource {
    id: i64,
    data: MediaSourceData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update mediasource's cursorState field
  UpdateMediaSourceCursorState {
    id: i64,
    cursorState: Option<serde_json::Value>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update mediasource's config field
  UpdateMediaSourceConfig {
    id: i64,
    config: Option<serde_json::Value>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update mediasource's lastPolledAt field
  UpdateMediaSourceLastPolledAt {
    id: i64,
    lastPolledAt: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update mediasource's lastError field
  UpdateMediaSourceLastError {
    id: i64,
    lastError: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update mediasource's updatedAt field
  UpdateMediaSourceUpdatedAt {
    id: i64,
    updatedAt: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete mediasource
  DeleteMediaSource {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // Person Commands
  // ============================================================================
  /// Create new person
  CreatePerson {
    data: PersonData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get person by ID
  ReadPersonById {
    id: i64,
    respond: oneshot::Sender<Result<Option<PersonData>>>,
  },
  
  /// List persons with optional filtering
  ListPersons {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: PersonFilter,
    respond: oneshot::Sender<Result<Vec<PersonData>>>,
  },
  
  /// Update person
  UpdatePerson {
    id: i64,
    data: PersonData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update person's handle field
  UpdatePersonHandle {
    id: i64,
    handle: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update person's avatarMediaId field
  UpdatePersonAvatarMediaId {
    id: i64,
    avatarMediaId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update person's metadata field
  UpdatePersonMetadata {
    id: i64,
    metadata: Option<serde_json::Value>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update person's updatedAt field
  UpdatePersonUpdatedAt {
    id: i64,
    updatedAt: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete person
  DeletePerson {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // Post Commands
  // ============================================================================
  /// Create new post
  CreatePost {
    data: PostData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get post by ID
  ReadPostById {
    id: i64,
    respond: oneshot::Sender<Result<Option<PostData>>>,
  },
  
  /// List posts with optional filtering
  ListPosts {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: PostFilter,
    respond: oneshot::Sender<Result<Vec<PostData>>>,
  },
  
  /// Update post
  UpdatePost {
    id: i64,
    data: PostData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update post's contentHash field
  UpdatePostContentHash {
    id: i64,
    contentHash: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update post's authorDid field
  UpdatePostAuthorDid {
    id: i64,
    authorDid: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update post's signature field
  UpdatePostSignature {
    id: i64,
    signature: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update post's updatedAt field
  UpdatePostUpdatedAt {
    id: i64,
    updatedAt: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete post
  DeletePost {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // StreamEntry Commands
  // ============================================================================
  /// Create new streamentry
  CreateStreamEntry {
    data: StreamEntryData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get streamentry by ID
  ReadStreamEntryById {
    id: i64,
    respond: oneshot::Sender<Result<Option<StreamEntryData>>>,
  },
  
  /// List streamentrys with optional filtering
  ListStreamEntrys {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: StreamEntryFilter,
    respond: oneshot::Sender<Result<Vec<StreamEntryData>>>,
  },
  
  /// Update streamentry
  UpdateStreamEntry {
    id: i64,
    data: StreamEntryData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's personId field
  UpdateStreamEntryPersonId {
    id: i64,
    personId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's postId field
  UpdateStreamEntryPostId {
    id: i64,
    postId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's mediaId field
  UpdateStreamEntryMediaId {
    id: i64,
    mediaId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's bookmarkId field
  UpdateStreamEntryBookmarkId {
    id: i64,
    bookmarkId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's conversationId field
  UpdateStreamEntryConversationId {
    id: i64,
    conversationId: Option<number>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's directory field
  UpdateStreamEntryDirectory {
    id: i64,
    directory: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's kind field
  UpdateStreamEntryKind {
    id: i64,
    kind: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update streamentry's contentHash field
  UpdateStreamEntryContentHash {
    id: i64,
    contentHash: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete streamentry
  DeleteStreamEntry {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  // ============================================================================
  // View Commands
  // ============================================================================
  /// Create new view
  CreateView {
    data: ViewData,
    respond: oneshot::Sender<Result<i64>>,
  },
  
  /// Get view by ID
  ReadViewById {
    id: i64,
    respond: oneshot::Sender<Result<Option<ViewData>>>,
  },
  
  /// List views with optional filtering
  ListViews {
    limit: Option<usize>,
    offset: Option<usize>,
    filter: ViewFilter,
    respond: oneshot::Sender<Result<Vec<ViewData>>>,
  },
  
  /// Update view
  UpdateView {
    id: i64,
    data: ViewData,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update view's label field
  UpdateViewLabel {
    id: i64,
    label: Option<string>,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Update view's updatedAt field
  UpdateViewUpdatedAt {
    id: i64,
    updatedAt: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  /// Delete view
  DeleteView {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  
  /// Graceful shutdown
  Shutdown,
}
