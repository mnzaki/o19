// GENERATED BY DBBINDING TREADLE - Do not edit

use crate::db::Result;
use crate::conversation::Conversation;
use crossbeam_channel::Sender as OneshotSender;

/// DbCommand variants for Conversation operations
#[derive(Debug)]
pub enum ConversationCommand {
  InsertConversation {
    url: String,
    title: Option<String>,
    notes: Option<String>,
    content_hash: Option<String>,
    respond: oneshot::Sender<Result<i64>>,
  },
  GetConversationById {
    id: i64,
    respond: oneshot::Sender<Result<Option<Conversation>>>,
  },
  GetConversationByUrl {
    url: String,
    respond: oneshot::Sender<Result<Option<Conversation>>>,
  },
  ListConversations {
    limit: Option<usize>,
    offset: Option<usize>,
    respond: oneshot::Sender<Result<Vec<Conversation>>>,
  },
  UpdateConversation {
    id: i64,
    title: Option<String>,
    notes: Option<String>,
    respond: oneshot::Sender<Result<bool>>,
  },
  DeleteConversation {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  DeleteConversationByUrl {
    url: String,
    respond: oneshot::Sender<Result<bool>>,
  },
}

// Re-export as part of DbCommand enum
// impl DbActor {
//   fn handle_conversation_command(&self, cmd: ConversationCommand) { ... }
// }
