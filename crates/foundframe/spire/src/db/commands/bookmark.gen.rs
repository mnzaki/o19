// GENERATED BY DBBINDING TREADLE - Do not edit

use crate::db::Result;
use crate::bookmark::Bookmark;
use crossbeam_channel::Sender as OneshotSender;

/// DbCommand variants for Bookmark operations
#[derive(Debug)]
pub enum BookmarkCommand {
  InsertBookmark {
    url: String,
    title: Option<String>,
    notes: Option<String>,
    content_hash: Option<String>,
    respond: oneshot::Sender<Result<i64>>,
  },
  GetBookmarkById {
    id: i64,
    respond: oneshot::Sender<Result<Option<Bookmark>>>,
  },
  GetBookmarkByUrl {
    url: String,
    respond: oneshot::Sender<Result<Option<Bookmark>>>,
  },
  ListBookmarks {
    limit: Option<usize>,
    offset: Option<usize>,
    respond: oneshot::Sender<Result<Vec<Bookmark>>>,
  },
  UpdateBookmark {
    id: i64,
    title: Option<String>,
    notes: Option<String>,
    respond: oneshot::Sender<Result<bool>>,
  },
  DeleteBookmark {
    id: i64,
    respond: oneshot::Sender<Result<bool>>,
  },
  DeleteBookmarkByUrl {
    url: String,
    respond: oneshot::Sender<Result<bool>>,
  },
}

// Re-export as part of DbCommand enum
// impl DbActor {
//   fn handle_bookmark_command(&self, cmd: BookmarkCommand) { ... }
// }
