# LATER

When generating for typescript projects, throw an error if the tsconfig is not
suitable and give suggestions. Most importantly it needs to be configured to
have a rootDir that includes the spire
