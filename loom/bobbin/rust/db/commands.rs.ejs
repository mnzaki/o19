// GENERATED BY db-binding Treadle - Do not edit
//
// MISSING METADATA: Placeholder implementation. Field metadata not available.

use crate::db::Result;

/// Database commands for <%= entity.name %>
#[derive(Debug)]
pub enum <%= entity.name %>Command {
  Insert {
    respond: tokio::sync::oneshot::Sender<Result<i64>>,
  },
  GetById {
    id: i64,
    respond: tokio::sync::oneshot::Sender<Result<Option<super::<%= entity.lower %>_data::<%= entity.name %>Data>>>,
  },
  List {
    limit: Option<usize>,
    offset: Option<usize>,
    respond: tokio::sync::oneshot::Sender<Result<Vec<super::<%= entity.lower %>_data::<%= entity.name %>Data>>>,
  },
  Update {
    id: i64,
    respond: tokio::sync::oneshot::Sender<Result<bool>>,
  },
  Delete {
    id: i64,
    respond: tokio::sync::oneshot::Sender<Result<bool>>,
  },
}

impl <%= entity.name %>Command {
  pub fn handle(&self, _conn: &mut rusqlite::Connection) -> Result<()> {
    match self {
      <%= entity.name %>Command::Insert { .. } => todo!("Insert <%= entity.lower %> - needs field metadata"),
      <%= entity.name %>Command::GetById { .. } => todo!("Get <%= entity.lower %> by id"),
      <%= entity.name %>Command::List { .. } => todo!("List <%= entity.lower %>s"),
      <%= entity.name %>Command::Update { .. } => todo!("Update <%= entity.lower %> - needs field metadata"),
      <%= entity.name %>Command::Delete { .. } => todo!("Delete <%= entity.lower %>"),
    }
  }
}
